---
phase: 08-dependabot-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - D-01
  - D-02
  - D-04
  - D-05

must_haves:
  truths:
    - "multer is removed from package.json (unused dependency with HIGH vulns)"
    - "npm audit on root shows zero high-severity vulnerabilities"
    - "Server starts successfully after dependency changes"
  artifacts:
    - path: "package.json"
      provides: "Root dependencies with multer removed and transitive vulns resolved"
      contains: "express"
    - path: "package-lock.json"
      provides: "Locked dependency tree with patched transitive deps"
  key_links:
    - from: "package.json"
      to: "server.js"
      via: "npm dependency resolution"
      pattern: "express|cors|dotenv"
---

<objective>
Fix all root-level (server-side) dependency vulnerabilities by removing the unused multer package and running npm audit fix to resolve transitive vulnerabilities in express, puppeteer, and other server dependencies.

Purpose: Eliminate HIGH and MEDIUM severity Dependabot alerts originating from root package.json dependencies (multer alerts 66/67/68, path-to-regexp 46/72, lodash 49/50/61/73/74, minimatch 65, qs 62, and transitive picomatch/yaml alerts).
Output: Clean root package.json and package-lock.json with zero high-severity audit findings. Server starts and functions correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-using-the-github-cli-check-dependabot-alerts-and-resolve-sec/08-CONTEXT.md
@package.json
@server.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused multer and run npm audit fix on root</name>
  <files>package.json, package-lock.json</files>
  <read_first>package.json, server.js, routes/download.js, routes/playlist.js, routes/settings.js</read_first>
  <action>
1. Verify multer is NOT imported anywhere in the codebase. Search all .js and .mjs files for "multer" imports. It should have zero usage -- it was listed as a dependency for "potential future uploads" but never wired in.

2. Remove multer from package.json:
   ```
   npm uninstall multer
   ```

3. Run npm audit to see current state of root vulnerabilities:
   ```
   npm audit
   ```

4. Run npm audit fix to auto-resolve what it can:
   ```
   npm audit fix
   ```

5. If any HIGH vulnerabilities remain after audit fix, run:
   ```
   npm audit fix --force
   ```
   Only use --force if regular audit fix leaves HIGH vulns. Check the output carefully -- --force may bump major versions of transitive deps.

6. Run npm audit again to confirm results. The goal is zero HIGH severity issues in root deps. MEDIUM/LOW that are transitive-only and cannot be resolved without breaking changes are acceptable in this plan (they will be addressed if the transitive parent gets bumped).

7. Verify server starts:
   ```
   timeout 10 node server.js || true
   ```
   Server should start without import errors or missing module errors. It will eventually timeout since there is no signal to stop it -- that is expected.

Per D-01: Fix ALL open alerts. Per D-02: Include transitive dependencies. Per D-04: Verify build passes. Per D-05: Start with npm audit fix.
  </action>
  <verify>
    <automated>cd /Users/ashley.rhodes/proj/scripts/learning-projects/suno-playlist-downloader && ! grep -q '"multer"' package.json && echo "PASS: multer removed" || echo "FAIL: multer still in package.json"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c '"multer"' package.json` returns 0 (multer removed)
- `npm audit --json | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); console.log(d.metadata?.vulnerabilities?.high || 0)"` returns 0 (zero high-severity root vulns)
- `timeout 10 node server.js 2>&1 | head -5` shows "Checking the following paths" or similar startup output (no crash on import)
  </acceptance_criteria>
  <done>multer removed from package.json, npm audit shows zero HIGH severity issues for root deps, server.js starts without errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> lockfile | Package versions resolved from public registry |
| transitive deps -> runtime | Vulnerable transitives could be exploited in server context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | npm packages | mitigate | Use npm audit to verify package integrity; lockfile pins exact versions |
| T-08-02 | Elevation of Privilege | multer (unused) | mitigate | Remove entirely -- unused dependency with 3 HIGH vulns is pure attack surface |
| T-08-03 | Denial of Service | path-to-regexp (transitive via express) | mitigate | npm audit fix resolves to patched version |
| T-08-04 | Information Disclosure | lodash prototype pollution (transitive via puppeteer) | mitigate | npm audit fix updates transitive chain |
</threat_model>

<verification>
1. `npm audit` in root directory shows zero HIGH severity vulnerabilities
2. `grep -c multer package.json` returns 0
3. Server starts: `timeout 10 node server.js` produces startup logs without crash
</verification>

<success_criteria>
- multer completely removed from root package.json and package-lock.json
- Zero HIGH severity vulnerabilities in `npm audit` output for root
- Server starts and logs its normal startup sequence
</success_criteria>

<output>
After completion, create `.planning/phases/08-using-the-github-cli-check-dependabot-alerts-and-resolve-sec/08-01-SUMMARY.md`
</output>
