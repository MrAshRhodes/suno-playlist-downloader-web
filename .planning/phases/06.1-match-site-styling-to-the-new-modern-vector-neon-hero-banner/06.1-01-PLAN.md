---
phase: 06.1-match-site-styling-to-the-new-modern-vector-neon-hero-banner
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/index.css
  - client/src/App.css
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13

must_haves:
  truths:
    - "Dark mode renders deep navy #0B0D1A background with navy-tinted #141828 card surfaces and neon purple #7B5EA7 accent"
    - "Light mode renders cool lavender-gray #F0F2F8 background with #E4E7F0 card surfaces and deep purple #6B4F96 accent"
    - "Buttons glow purple on hover, progress bar glows cyan — dual accent system works contextually"
    - "No rgba(59,74,107,...) values remain anywhere in client/src/"
    - "Card borders show faint purple tint in both themes"
    - "Toggling theme causes no flash — cross-fade is smooth with no unstyled frames"
  artifacts:
    - path: "client/src/index.css"
      provides: "Updated CSS variable values for both dark and light themes"
      contains: "--accent-secondary"
    - path: "client/src/App.css"
      provides: "Updated hardcoded rgba values and secondary accent usage for progress/loader"
      contains: "var(--accent-secondary)"
  key_links:
    - from: "client/src/index.css"
      to: "client/src/App.css"
      via: "CSS custom properties consumed by var() references"
      pattern: "var\\(--accent-secondary\\)"
    - from: "client/src/App.css .progress-fill"
      to: "client/src/index.css --accent-secondary"
      via: "var(--accent-secondary) for cyan progress bar"
      pattern: "background.*var\\(--accent-secondary\\)"
---

<objective>
Harmonize the full app color palette with the neon navy/purple/cyan hero banner by updating CSS variable values in both theme blocks and replacing hardcoded rgba values in component styles.

Purpose: The hero banner (Phase 6) introduced a dark navy/purple/cyan design language that clashes with the existing Rich Black/steel-blue palette. This plan swaps every color value to create visual cohesion.

Output: Updated `client/src/index.css` (both theme variable blocks) and `client/src/App.css` (hardcoded values + dual accent wiring). No new files, no new components, no layout changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-match-site-styling-to-the-new-modern-vector-neon-hero-banner/06.1-CONTEXT.md
@.planning/phases/06.1-match-site-styling-to-the-new-modern-vector-neon-hero-banner/06.1-RESEARCH.md
@.planning/phases/06.1-match-site-styling-to-the-new-modern-vector-neon-hero-banner/06.1-UI-SPEC.md

<interfaces>
<!-- Current CSS variable system in index.css — executor must read current state before editing -->
<!-- Variable names are PRESERVED — only values change. Two NEW variables added. -->

From client/src/index.css (dark-mode block, lines 27-51):
```css
:root.dark-mode {
  --bg-primary: #0A0A0A;          /* -> #0B0D1A */
  --bg-secondary: #111111;        /* -> #0E1020 */
  --bg-card: #1A1A1A;             /* -> #141828 */
  --bg-card-hover: #222222;       /* -> #1C2035 */
  --accent: #3B4A6B;              /* -> #7B5EA7 */
  --accent-hover: #4E5F80;        /* -> #8E71BA */
  --accent-active: #2F3D58;       /* -> #6A4F91 */
  --accent-glow: rgba(59,...);    /* -> rgba(123,94,167,0.25) */
  --border-color: rgba(255,...);  /* -> rgba(123,94,167,0.15) */
  --border-subtle: rgba(255,...); /* -> rgba(123,94,167,0.08) */
  --shadow-button: ...;           /* -> rgba(123,94,167,0.30) */
  --progress-glow: ...;           /* -> rgba(0,200,255,0.50) */
  --scrollbar-thumb: ...;         /* -> rgba(123,94,167,0.25) */
  --scrollbar-hover: ...;         /* -> rgba(123,94,167,0.40) */
  /* NEW: --accent-secondary: #00C8FF */
  /* NEW: --accent-secondary-glow: rgba(0,200,255,0.20) */
}
```

From client/src/App.css (hardcoded values to replace):
```css
/* Line 59 */ box-shadow: var(--shadow-button), 0 0 12px rgba(59, 74, 107, 0.3);
/* Line 65 */ box-shadow: var(--shadow-button), 0 0 12px rgba(59, 74, 107, 0.15);
/* Line 396 */ box-shadow: var(--shadow-button), 0 0 12px rgba(59, 74, 107, 0.3) !important;
/* Line 400 */ box-shadow: var(--shadow-button), 0 0 12px rgba(59, 74, 107, 0.15) !important;
```

From client/src/App.css (progress/loader rules to switch to secondary accent):
```css
/* Line 183 */ .progress-fill { background: var(--accent); }
/* Line 404 */ .mantine-Loader-root { stroke: var(--accent) !important; }
/* Line 408 */ .mantine-Progress-bar { background-color: var(--accent) !important; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CSS variable values in both theme blocks</name>
  <files>client/src/index.css</files>
  <read_first>
    - client/src/index.css (current variable values — the file being modified)
    - .planning/phases/06.1-match-site-styling-to-the-new-modern-vector-neon-hero-banner/06.1-UI-SPEC.md (authoritative color contract)
  </read_first>
  <action>
Replace the entire dark-mode and light-mode variable blocks in `client/src/index.css` (lines 27-78) with new values per decisions D-01 through D-13. Add two NEW variables (`--accent-secondary`, `--accent-secondary-glow`) to each block.

**Dark mode block (:root.dark-mode, lines 27-51) — replace with these exact values:**

```css
/* --- Dark mode (default / primary) --- */
:root.dark-mode {
  --bg-primary: #0B0D1A;
  --bg-secondary: #0E1020;
  --bg-card: #141828;
  --bg-card-hover: #1C2035;
  --bg-input: rgba(255, 255, 255, 0.06);
  --text-primary: #FFFFFF;
  --text-secondary: #B3B3B3;
  --text-muted: rgba(255, 255, 255, 0.50);
  --accent: #7B5EA7;
  --accent-hover: #8E71BA;
  --accent-active: #6A4F91;
  --accent-glow: rgba(123, 94, 167, 0.25);
  --accent-secondary: #00C8FF;
  --accent-secondary-glow: rgba(0, 200, 255, 0.20);
  --border-color: rgba(123, 94, 167, 0.15);
  --border-subtle: rgba(123, 94, 167, 0.08);
  --shadow-card: inset 0 1px 0 rgba(255,255,255,0.05), 0 4px 24px rgba(0,0,0,0.4);
  --shadow-button: 0 2px 8px rgba(123, 94, 167, 0.30);
  --banner-bg: rgba(255, 183, 77, 0.08);
  --banner-border: rgba(255, 183, 77, 0.2);
  --banner-text: #ffb74d;
  --progress-track: rgba(255, 255, 255, 0.06);
  --progress-glow: 0 0 8px rgba(0, 200, 255, 0.50);
  --scrollbar-thumb: rgba(123, 94, 167, 0.25);
  --scrollbar-hover: rgba(123, 94, 167, 0.40);
}
```

Key changes (per D-01): bg #0A0A0A -> #0B0D1A, card #1A1A1A -> #141828
Key changes (per D-04): accent #3B4A6B -> #7B5EA7 (purple), hover/active/glow updated
Key changes (per D-05): NEW --accent-secondary: #00C8FF, --accent-secondary-glow: rgba(0,200,255,0.20)
Key changes (per D-08): border-color from white-based to purple-tinted rgba(123,94,167,0.15)

**Light mode block (:root.light-mode, lines 54-78) — replace with these exact values:**

```css
/* --- Light mode --- */
:root.light-mode {
  --bg-primary: #F0F2F8;
  --bg-secondary: #E8EAF2;
  --bg-card: #E4E7F0;
  --bg-card-hover: #D8DCE8;
  --bg-input: rgba(0, 0, 0, 0.04);
  --text-primary: #1A1A2E;
  --text-secondary: #5A5A7A;
  --text-muted: rgba(26, 26, 46, 0.60);
  --accent: #6B4F96;
  --accent-hover: #7D60A8;
  --accent-active: #5A4080;
  --accent-glow: rgba(107, 79, 150, 0.15);
  --accent-secondary: #0099CC;
  --accent-secondary-glow: rgba(0, 153, 204, 0.15);
  --border-color: rgba(107, 79, 150, 0.12);
  --border-subtle: rgba(107, 79, 150, 0.06);
  --shadow-card: 0 2px 8px rgba(0,0,0,0.06), 0 8px 24px rgba(0,0,0,0.04);
  --shadow-button: 0 2px 8px rgba(107, 79, 150, 0.20);
  --banner-bg: rgba(255, 152, 0, 0.08);
  --banner-border: rgba(255, 152, 0, 0.25);
  --banner-text: #bf360c;
  --progress-track: rgba(0, 0, 0, 0.06);
  --progress-glow: 0 0 8px rgba(0, 153, 204, 0.40);
  --scrollbar-thumb: rgba(107, 79, 150, 0.20);
  --scrollbar-hover: rgba(107, 79, 150, 0.35);
}
```

Key changes (per D-10): bg #F2EFE9 -> #F0F2F8, card #E8E4DB -> #E4E7F0
Key changes (per D-11): text-primary #332F2E -> #1A1A2E
Key changes (per D-12): accent #3B4A6B -> #6B4F96, secondary #0099CC
Key changes (per D-13): border-color from black-based to purple-tinted rgba(107,79,150,0.12)

**Do NOT change:** Lines 1-25 (:root base styles, body, #root). Only the two theme blocks change. Variable NAMES are preserved exactly. Only VALUES change. Two new variable names added.
  </action>
  <verify>
    <automated>grep -c "accent-secondary" client/src/index.css | grep -q "4" && echo "PASS: 4 accent-secondary declarations" || echo "FAIL"; grep "#0B0D1A" client/src/index.css && echo "PASS: dark bg updated" || echo "FAIL"; grep "#F0F2F8" client/src/index.css && echo "PASS: light bg updated" || echo "FAIL"; grep "#7B5EA7" client/src/index.css && echo "PASS: dark accent purple" || echo "FAIL"; grep "#6B4F96" client/src/index.css && echo "PASS: light accent purple" || echo "FAIL"; grep "rgba(59" client/src/index.css && echo "FAIL: old steel-blue values remain in index.css" || echo "PASS: no old rgba(59) in index.css"</automated>
  </verify>
  <acceptance_criteria>
    - client/src/index.css contains `--bg-primary: #0B0D1A` in dark-mode block
    - client/src/index.css contains `--bg-primary: #F0F2F8` in light-mode block
    - client/src/index.css contains `--accent: #7B5EA7` in dark-mode block
    - client/src/index.css contains `--accent: #6B4F96` in light-mode block
    - client/src/index.css contains `--accent-secondary: #00C8FF` in dark-mode block
    - client/src/index.css contains `--accent-secondary: #0099CC` in light-mode block
    - client/src/index.css contains `--accent-secondary-glow` in both theme blocks (4 total accent-secondary lines)
    - client/src/index.css contains `--border-color: rgba(123, 94, 167, 0.15)` in dark-mode block
    - client/src/index.css contains `--border-color: rgba(107, 79, 150, 0.12)` in light-mode block
    - `grep "rgba(59" client/src/index.css` returns zero matches (all old steel-blue values replaced)
    - client/src/index.css contains `--text-primary: #1A1A2E` in light-mode block
    - client/src/index.css contains `--scrollbar-thumb: rgba(123, 94, 167, 0.25)` in dark-mode block
    - Lines 1-25 (base styles) are unchanged
  </acceptance_criteria>
  <done>Both theme variable blocks contain the complete new palette per D-01 to D-13. Two new accent-secondary variables present in each block. Zero rgba(59,...) values remain in index.css.</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded rgba values and wire dual accent in App.css</name>
  <files>client/src/App.css</files>
  <read_first>
    - client/src/App.css (current component styles — the file being modified)
    - .planning/phases/06.1-match-site-styling-to-the-new-modern-vector-neon-hero-banner/06.1-UI-SPEC.md (hardcoded replacement table and dual accent rules)
  </read_first>
  <action>
Three sets of changes in `client/src/App.css`:

**Change Set A — Replace 4 hardcoded rgba(59,74,107,...) values with purple equivalent:**

Per D-04 and UI-SPEC Hardcoded Value Replacements table:

1. Line 59 (`.btn-accent:hover:not(:disabled)` box-shadow):
   Replace: `0 0 12px rgba(59, 74, 107, 0.3)`
   With:    `0 0 12px rgba(123, 94, 167, 0.30)`

2. Line 65 (`.btn-accent:active:not(:disabled)` box-shadow):
   Replace: `0 0 12px rgba(59, 74, 107, 0.15)`
   With:    `0 0 12px rgba(123, 94, 167, 0.15)`

3. Line 396 (`.mantine-Button-root[data-variant="filled"]:hover` box-shadow):
   Replace: `0 0 12px rgba(59, 74, 107, 0.3) !important`
   With:    `0 0 12px rgba(123, 94, 167, 0.30) !important`

4. Line 400 (`.mantine-Button-root[data-variant="filled"]:active` box-shadow):
   Replace: `0 0 12px rgba(59, 74, 107, 0.15) !important`
   With:    `0 0 12px rgba(123, 94, 167, 0.15) !important`

**Change Set B — Switch progress/loader to secondary accent per D-05 and D-06:**

1. Line 183 (`.progress-fill` background):
   Replace: `background: var(--accent);`
   With:    `background: var(--accent-secondary);`

2. Line 404 (`.mantine-Loader-root` stroke):
   Replace: `stroke: var(--accent) !important;`
   With:    `stroke: var(--accent-secondary) !important;`

3. Line 408 (`.mantine-Progress-bar` background-color):
   Replace: `background-color: var(--accent) !important;`
   With:    `background-color: var(--accent-secondary) !important;`

**Change Set C — No other changes:**
Do NOT modify any other rules. The `.step-number` keeps `var(--accent)` (purple — it is a UI element not a progress indicator, per RESEARCH.md Open Question Q3). The `.model-badge` keeps `var(--accent)`. The `.app-footer a` keeps `var(--accent)`. The `.input-field:focus` keeps `var(--accent)` and `var(--accent-glow)`.

All other rules reference CSS variables which already received updated values via Task 1.
  </action>
  <verify>
    <automated>grep -r "rgba(59" client/src/ && echo "FAIL: old steel-blue rgba values remain" || echo "PASS: zero rgba(59) in client/src/"; grep "accent-secondary" client/src/App.css | wc -l | xargs -I{} sh -c 'if [ {} -eq 3 ]; then echo "PASS: 3 accent-secondary refs in App.css"; else echo "FAIL: expected 3 accent-secondary refs, found {}"; fi'; grep "progress-fill" client/src/App.css | grep "accent-secondary" && echo "PASS: progress-fill uses secondary" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "rgba(59" client/src/` returns zero matches across ALL files (index.css cleaned in Task 1, App.css cleaned here)
    - client/src/App.css `.btn-accent:hover` box-shadow contains `rgba(123, 94, 167, 0.30)`
    - client/src/App.css `.btn-accent:active` box-shadow contains `rgba(123, 94, 167, 0.15)`
    - client/src/App.css `.mantine-Button-root[data-variant="filled"]:hover` contains `rgba(123, 94, 167, 0.30)`
    - client/src/App.css `.mantine-Button-root[data-variant="filled"]:active` contains `rgba(123, 94, 167, 0.15)`
    - client/src/App.css `.progress-fill` contains `background: var(--accent-secondary)`
    - client/src/App.css `.mantine-Loader-root` contains `stroke: var(--accent-secondary) !important`
    - client/src/App.css `.mantine-Progress-bar` contains `background-color: var(--accent-secondary) !important`
    - client/src/App.css `.step-number` still contains `background: var(--accent)` (NOT accent-secondary)
  </acceptance_criteria>
  <done>All 4 hardcoded rgba(59,74,107,...) replaced with purple equivalents. Progress fill, Mantine Progress bar, and Mantine Loader switched from primary to secondary accent. Step numbers remain on primary accent. Zero old steel-blue values in the entire client/src/ directory.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of both themes</name>
  <files>client/src/index.css, client/src/App.css</files>
  <read_first>
    - client/src/index.css (verify variable values are correct)
    - client/src/App.css (verify hardcoded values replaced and secondary accent wired)
  </read_first>
  <action>
Human visual verification of completed palette harmonization. Run `cd client && npm run dev` and inspect both themes in browser at http://localhost:5173.

**What was built:**
Complete palette harmonization: dark navy/purple/cyan theme and cool lavender-gray/purple/cyan light theme. Dual accent system where buttons glow purple and progress bars glow cyan. Card borders have faint purple tint in both modes.

**Verification steps:**
1. Run `cd client && npm run dev` to start the dev server
2. Open http://localhost:5173 in a browser
3. **Dark mode check:**
   - Page background should be deep navy (#0B0D1A), not near-black
   - Cards should be navy-tinted (#141828), not dark gray
   - Buttons should be neon purple (#7B5EA7)
   - Hover a button — should glow purple, not steel-blue
   - If a progress bar is visible, it should be cyan (#00C8FF) not purple
   - Card borders should have a faint purple tint
   - Hero banner should sit naturally on the navy background
4. **Light mode check (toggle theme):**
   - Page background should be cool lavender-gray (#F0F2F8), not warm ivory
   - Cards should be #E4E7F0, not warm beige
   - Buttons should be deep purple (#6B4F96)
   - Text should be dark navy (#1A1A2E), not warm brown
5. **Contrast check:**
   - All text should be readable in both modes
   - Muted text (tags, hints) should be readable against card backgrounds
   - Orange support banner text should be readable against both backgrounds
6. **No regression:**
   - Theme toggle transitions smoothly — no flash
   - All interactive elements (buttons, inputs, table rows) respond to hover
   - Scrollbar matches theme colors

**Resume signal:** Type "approved" or describe specific issues to fix
  </action>
  <verify>
    <automated>grep -r "rgba(59" client/src/ && echo "FAIL" || echo "PASS: no old values"; cd client && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - User has visually confirmed dark mode renders navy background, purple buttons, cyan progress
    - User has visually confirmed light mode renders lavender-gray background, purple buttons
    - User has confirmed text is readable in both modes (WCAG AA)
    - User has confirmed theme toggle transitions smoothly
    - `npm run build` in client/ succeeds with zero errors
  </acceptance_criteria>
  <done>User has visually confirmed both themes render correctly with the new palette. Dark mode shows navy/purple/cyan. Light mode shows lavender-gray/purple/cyan. Dual accent works. No regressions. WCAG AA contrast maintained.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS values only | No user input flows into CSS — all values are hardcoded in source files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6.1-01 | Tampering | CSS variable values | accept | CSS files are build-time artifacts served as static assets. No runtime injection vector. Values are author-controlled hex/rgba literals. |
| T-6.1-02 | Information Disclosure | Color values | accept | Color hex codes are not sensitive data. No PII, no secrets in CSS. |

No high-severity threats identified. This phase modifies only CSS variable values and hardcoded rgba color literals — zero attack surface expansion.
</threat_model>

<verification>
1. `grep -r "rgba(59" client/src/` returns zero matches — all old steel-blue values eliminated
2. `grep -c "accent-secondary" client/src/index.css` returns 4 — two variable declarations per theme block
3. `grep -c "accent-secondary" client/src/App.css` returns 3 — progress-fill, Mantine Loader, Mantine Progress bar
4. `grep "#0B0D1A" client/src/index.css` confirms dark background updated
5. `grep "#F0F2F8" client/src/index.css` confirms light background updated
6. `grep "#7B5EA7" client/src/index.css` confirms dark accent is purple
7. `grep "#6B4F96" client/src/index.css` confirms light accent is purple
8. `npm run build` (from client/) succeeds with no errors — CSS is valid
9. Visual inspection confirms dual accent system (purple buttons, cyan progress)
</verification>

<success_criteria>
- Both theme blocks in index.css contain the complete new palette (D-01 through D-13 fully implemented)
- Dual accent system works: buttons/actions are purple, progress/loader are cyan
- Zero hardcoded rgba(59,74,107,...) values remain in client/src/
- Both themes render correctly with no visual regressions
- Theme toggle transitions smoothly
- WCAG AA contrast maintained for all text/background combinations
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-match-site-styling-to-the-new-modern-vector-neon-hero-banner/06.1-01-SUMMARY.md`
</output>
