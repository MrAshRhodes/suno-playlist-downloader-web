---
phase: 06-premium-title-banner-and-modern-step-cards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/assets/hero-banner.png
  - client/src/App.tsx
  - client/src/App.css
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06

must_haves:
  truths:
    - "A hero banner image with title overlay renders below the Support Server Costs banner"
    - "The Support Server Costs banner at the top of the page is unchanged"
    - "All 3 step sections are wrapped in monolith-card containers with step number circles"
    - "Step number circles use solid var(--accent) background (no gradients)"
    - "Banner and cards render correctly in both dark and light mode"
    - "The app builds without errors via npm run build"
  artifacts:
    - path: "client/src/assets/hero-banner.png"
      provides: "Generated hero banner image (music/vinyl/audio theme, warm tones)"
    - path: "client/src/App.tsx"
      provides: "Hero banner JSX + step card wrapper JSX with numbered headings"
      contains: "hero-banner"
    - path: "client/src/App.css"
      provides: "Hero banner CSS + step card CSS + step number CSS"
      contains: ".hero-banner"
  key_links:
    - from: "client/src/App.tsx"
      to: "client/src/assets/hero-banner.png"
      via: "import heroBannerImg from './assets/hero-banner.png'"
      pattern: "import heroBannerImg"
    - from: "client/src/App.tsx"
      to: "client/src/App.css"
      via: "className hero-banner, step-card, step-number"
      pattern: "step-card monolith-card"
---

<objective>
Replace the plain header with a generated hero banner image and wrap all 3 step sections in monolith-card containers with solid-surface step number indicators.

Purpose: Visual upgrade — the app gets a premium hero header and structured step cards while preserving all existing functionality.
Output: hero-banner.png asset, updated App.tsx layout, updated App.css styles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-premium-title-banner-and-modern-step-cards/06-CONTEXT.md
@.planning/phases/06-premium-title-banner-and-modern-step-cards/06-RESEARCH.md
@client/src/App.tsx
@client/src/App.css
@client/src/index.css

<interfaces>
<!-- Existing CSS classes and variables the executor needs -->

From client/src/App.css:
```css
.monolith-card {
  background: var(--bg-card);
  border: 1px solid var(--border-color);
  border-radius: 24px;
  box-shadow: var(--shadow-card);
  transition: background-color 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease;
}

.section-heading {
  color: var(--text-primary);
  font-size: 20px;
  font-weight: 600;
  margin: 24px 0 12px;
  letter-spacing: -0.02em;
}

.app-header { /* BEING REPLACED — remove or repurpose */ }
.info-banner { /* BEING ABSORBED into hero — remove or repurpose */ }
```

From client/src/index.css (CSS variables available):
```css
--bg-card, --bg-card-hover, --border-color, --border-subtle
--accent (#3B4A6B), --accent-hover (#4E5F80)
--shadow-card, --text-primary, --text-secondary, --text-muted
```

From client/src/components/DonationModal.tsx (asset import pattern):
```tsx
import bannerImg from '../assets/donation-banner.png';
// Then: <Image src={bannerImg} ... />
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0: Generate hero-banner.png via nanobanana MCP</name>
  <read_first>
    client/src/assets/donation-banner.png
    .planning/phases/06-premium-title-banner-and-modern-step-cards/06-RESEARCH.md
  </read_first>
  <action>
Generate a hero banner image using nanobanana MCP (available at orchestrator level only — NOT inside executor subagents).

Image requirements:
- Theme: music/vinyl/audio scene, warm dark tones (similar mood to donation-banner.png)
- Dimensions: wide landscape, approximately 1200x400px (will be displayed at full-width, 220px height with object-fit: cover)
- Style: moody, atmospheric, not corporate — think vinyl records, audio waves, warm amber/brown tones against dark background
- Must look good when cropped to a wide strip (3:1 aspect ratio display)
- Must be legible with white text overlaid (dark enough base or naturally dark)

Save to: `client/src/assets/hero-banner.png`

If nanobanana is unavailable, generate a fallback with ImageMagick:
```bash
convert -size 1200x400 \
  -define gradient:angle=135 \
  gradient:'#1a1520'-'#2d2035' \
  -fill '#3B4A6B' -draw "roundrectangle 100,150 400,350 20,20" \
  -fill '#4E5F80' -draw "circle 250,250 250,200" \
  -gaussian-blur 0x15 \
  client/src/assets/hero-banner.png
```
  </action>
  <verify>
    <automated>test -f client/src/assets/hero-banner.png && echo "hero-banner.png exists" || echo "MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - File `client/src/assets/hero-banner.png` exists
    - File size is greater than 10KB (not a stub)
    - Image is landscape orientation (wider than tall)
  </acceptance_criteria>
  <done>hero-banner.png exists in client/src/assets/ and is a valid landscape image with warm/dark tones suitable for text overlay</done>
</task>

<task type="auto">
  <name>Task 1: Hero banner — replace app-header and info-banner with image hero</name>
  <read_first>
    client/src/App.tsx
    client/src/App.css
    client/src/index.css
    client/src/components/DonationModal.tsx
  </read_first>
  <files>client/src/App.tsx, client/src/App.css</files>
  <action>
**In App.tsx:**

1. Add import at top (after existing imports, before component function):
```tsx
import heroBannerImg from './assets/hero-banner.png';
```

2. Replace the `{/* Header */}` block (lines 162-173, the `<header className="app-header">...</header>`) AND the `{/* Info banner */}` block (lines 175-179, the `<div className="info-banner">...</div>`) with a single hero banner section. The ThemeToggle must be preserved — move it to an absolute-positioned spot inside the hero banner.

Replace from `{/* Header */}` through end of `{/* Info banner */}` div with:
```tsx
{/* Hero banner — per D-01, D-02, D-05 (support-banner stays above, untouched) */}
<div className="hero-banner">
  <img src={heroBannerImg} alt="" className="hero-banner-img" aria-hidden="true" />
  <div className="hero-overlay" />
  <div className="hero-content">
    <h1 className="app-title">Suno Playlist Downloader</h1>
    <p className="hero-subtitle">
      Download music from your Suno playlists directly to your device.
      Files will be saved to your browser's default download location.
    </p>
  </div>
  <div className="hero-actions">
    <ThemeToggle theme={theme} toggleTheme={toggleTheme} />
  </div>
</div>
```

3. Remove the `IconVinyl` and `IconInfoCircle` imports since they are no longer used. Keep all other imports unchanged. The `IconVinyl` import is on line 7, `IconInfoCircle` on line 8 — remove both from the destructured import. (If IconInfoCircle or IconVinyl are used elsewhere, keep them — verify by searching the file.)

**In App.css:**

4. Replace the existing `.app-header` block (lines 246-251) and its children `.app-logo`, `.app-logo img`, `.app-logo-fallback`, `.app-title` (lines 253-283) with new hero banner styles. Also replace `.info-banner` block (lines 286-302).

Remove these CSS blocks entirely:
- `.app-header` (the flex container)
- `.app-logo` (width/height 38px)
- `.app-logo img` (border-radius 11px)
- `.app-logo-fallback` (accent background circle)
- `.info-banner` (the card-style info section)
- `.info-banner p` (margin/color/font)

Keep `.app-title` but modify it for hero context.

Add these new CSS blocks:

```css
/* ─── Hero banner ─── */
.hero-banner {
  position: relative;
  height: 220px;
  border-radius: 20px;
  overflow: hidden;
  margin-bottom: 24px;
}

.hero-banner-img {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;
}

.hero-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(
    to bottom,
    rgba(0, 0, 0, 0.25) 0%,
    rgba(0, 0, 0, 0.55) 100%
  );
}

.hero-content {
  position: relative;
  z-index: 1;
  padding: 24px 28px;
  height: 100%;
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
}

.hero-content .app-title {
  color: #ffffff;
  text-shadow: 0 1px 4px rgba(0, 0, 0, 0.5);
  margin: 0 0 6px;
  font-size: 24px;
  font-weight: 600;
  letter-spacing: -0.03em;
}

.hero-subtitle {
  color: rgba(255, 255, 255, 0.80);
  font-size: 14px;
  margin: 0;
  line-height: 1.5;
  text-shadow: 0 1px 3px rgba(0, 0, 0, 0.4);
  max-width: 500px;
}

.hero-actions {
  position: absolute;
  top: 16px;
  right: 16px;
  z-index: 2;
}
```

5. Retain a standalone `.app-title` rule only if it is used outside the hero (check App.tsx — it is not). If the only `.app-title` usage is inside `.hero-content`, the `.hero-content .app-title` rule above is sufficient and the old standalone `.app-title` block can be removed.

**CRITICAL per D-05:** Do NOT touch the `.support-banner` JSX or CSS. It must remain exactly as-is above the hero banner.

**Theme note per D-06:** Hero text is hardcoded white (not var(--text-primary)) because it renders over the image in both themes. The overlay gradient is always dark.
  </action>
  <verify>
    <automated>cd /Users/ashley.rhodes/proj/scripts/learning-projects/suno-playlist-downloader/client && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx contains `import heroBannerImg from './assets/hero-banner.png'`
    - App.tsx contains `className="hero-banner"`
    - App.tsx contains `className="hero-overlay"`
    - App.tsx contains `className="hero-content"`
    - App.tsx contains `className="hero-subtitle"`
    - App.tsx contains `className="hero-actions"`
    - App.tsx contains `<ThemeToggle` inside the hero-actions div
    - App.tsx does NOT contain `className="app-header"` (old header removed)
    - App.tsx does NOT contain `className="info-banner"` (old info section removed)
    - App.tsx still contains `className="support-banner"` (untouched per D-05)
    - App.css contains `.hero-banner {` with `height: 220px`
    - App.css contains `.hero-overlay {`
    - App.css contains `.hero-content .app-title {` with `color: #ffffff`
    - App.css contains `.hero-subtitle {` with `color: rgba(255, 255, 255, 0.80)`
    - App.css contains `.hero-actions {` with `position: absolute`
    - App.css does NOT contain `.app-header {` (old header CSS removed)
    - App.css does NOT contain `.info-banner {` (old info CSS removed)
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>
    Hero banner replaces old app-header and info-banner. Banner shows generated image with title overlay at 220px height.
    ThemeToggle moved to top-right of hero. Support banner untouched above. Builds clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Step card wrappers with numbered heading indicators</name>
  <read_first>
    client/src/App.tsx
    client/src/App.css
    client/src/components/SectionHeading.tsx
  </read_first>
  <files>client/src/App.tsx, client/src/App.css</files>
  <action>
**In App.tsx:**

Wrap each of the 3 step sections in a `.step-card.monolith-card` container and replace the bare `<h3>` headings with `.step-heading` rows containing numbered circles.

**Step 1 (currently lines ~182-199 after Task 1 edits):**

Replace:
```tsx
<h3 className="section-heading">1. Paste playlist link</h3>
<div style={{ display: "flex", gap: "10px", marginBottom: "24px" }}>
  ...input + button...
</div>
```

With:
```tsx
{/* Step 1: Paste link — per D-03, D-04 */}
<div className="step-card monolith-card">
  <div className="step-heading">
    <div className="step-number">1</div>
    <h3 className="section-heading" style={{ margin: 0 }}>Paste playlist link</h3>
  </div>
  <div style={{ display: "flex", gap: "10px" }}>
    <input
      type="text"
      value={playlistUrl}
      onChange={(e) => setPlaylistUrl(e.target.value)}
      placeholder="https://suno.com/playlist/..."
      disabled={isGettingPlaylist || isDownloading}
      className="input-field"
    />
    <button
      onClick={getPlaylist}
      disabled={isGettingPlaylist || isDownloading}
      className="btn-accent"
    >
      Get playlist songs
    </button>
  </div>
</div>
```

**Step 2 (currently lines ~202-240 after Task 1 edits):**

Replace:
```tsx
<h3 className="section-heading">2. Review songs</h3>
<div className="monolith-card" style={{ marginBottom: "24px", padding: "0", maxHeight: "340px", overflowY: "auto" }}>
  <table ref={songTable} className="song-table">...</table>
</div>
```

With:
```tsx
{/* Step 2: Review songs — per D-03, D-04 */}
<div className="step-card monolith-card">
  <div className="step-heading">
    <div className="step-number">2</div>
    <h3 className="section-heading" style={{ margin: 0 }}>Review songs</h3>
  </div>
  <div className="monolith-card song-table-card" style={{ padding: 0, maxHeight: "340px", overflowY: "auto" }}>
    <table ref={songTable} className="song-table">
      <thead>
        <tr>
          <th>Img</th>
          <th>Title</th>
          <th style={{ textAlign: "right" }}>Length</th>
          <th style={{ textAlign: "center" }}>Status</th>
        </tr>
      </thead>
      <tbody>
        {playlistData && playlistClips?.map((clip) => (
          <tr key={clip.id} data-id={`row-${clip.id}`}>
            <td style={{ width: "50px" }}>
              <img
                src={clip.image_url}
                alt=""
                style={{ width: "40px", height: "40px", borderRadius: "6px", objectFit: "cover" }}
              />
            </td>
            <td>
              <div>
                <strong>{clip.title}</strong>
                <span className="model-badge">{clip.model_version}</span>
              </div>
              <div className="tag-text">{clip.tags}</div>
            </td>
            <td style={{ textAlign: "right", fontFamily: "monospace" }}>
              {formatSecondsToTime(clip.duration)}
            </td>
            <td style={{ textAlign: "center" }}>
              <StatusIcon status={clip.status} />
            </td>
          </tr>
        ))}
      </tbody>
    </table>
  </div>
</div>
```

**Step 3 (currently lines ~243-254 after Task 1 edits):**

Replace:
```tsx
<h3 className="section-heading">3. Download playlist</h3>
<div style={{ display: "flex", justifyContent: "flex-end", marginBottom: "24px" }}>
  ...download button...
</div>
```

With:
```tsx
{/* Step 3: Download — per D-03, D-04 */}
<div className="step-card monolith-card">
  <div className="step-heading">
    <div className="step-number">3</div>
    <h3 className="section-heading" style={{ margin: 0 }}>Download playlist</h3>
  </div>
  <div style={{ display: "flex", justifyContent: "flex-end" }}>
    <button
      onClick={downloadPlaylist}
      disabled={isGettingPlaylist || isDownloading || !playlistData}
      className="btn-accent"
      style={{ display: "flex", alignItems: "center", gap: "8px" }}
    >
      <IconDownload size={18} />
      Download as ZIP
    </button>
  </div>
</div>
```

Note: Remove the "1. ", "2. ", "3. " number prefixes from the heading text since numbers are now in the circle indicators. The headings become just "Paste playlist link", "Review songs", "Download playlist".

Note: The old `marginBottom: "24px"` on each step's content div is no longer needed — the `.step-card` class provides consistent bottom margin.

**In App.css:**

Add these new CSS blocks (after the hero banner section):

```css
/* ─── Step cards ─── */
.step-card {
  padding: 24px;
  margin-bottom: 20px;
}

.step-heading {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-bottom: 16px;
}

.step-number {
  width: 32px;
  height: 32px;
  border-radius: 50%;
  background: var(--accent);
  color: #ffffff;
  font-size: 15px;
  font-weight: 600;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.step-heading .section-heading {
  margin: 0;
}

/* Inner table card inside Step 2 — reduced radius, subtle border */
.song-table-card {
  border-radius: 16px;
  border-color: var(--border-subtle);
  margin-bottom: 0;
}
```

**CRITICAL per D-04:** The `.step-number` uses `background: var(--accent)` — a solid fill. Do NOT use `linear-gradient` or any gradient pattern from SectionHeading.tsx. That component uses `background: 'linear-gradient(135deg, #0071e3, #40a9ff)'` which violates the Monolith design system.

**Theme handling per D-06:** `.step-number` uses `var(--accent)` which is `#3B4A6B` in both themes. `.step-card` inherits `.monolith-card` which already uses `var(--bg-card)` and `var(--shadow-card)` — theme-aware by default.
  </action>
  <verify>
    <automated>cd /Users/ashley.rhodes/proj/scripts/learning-projects/suno-playlist-downloader/client && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx contains `className="step-card monolith-card"` (3 occurrences, one per step)
    - App.tsx contains `className="step-number"` (3 occurrences)
    - App.tsx contains `className="step-heading"` (3 occurrences)
    - App.tsx contains `className="song-table-card"` (in Step 2 inner card)
    - App.tsx heading text is "Paste playlist link" (not "1. Paste playlist link")
    - App.tsx heading text is "Review songs" (not "2. Review songs")
    - App.tsx heading text is "Download playlist" (not "3. Download playlist")
    - App.css contains `.step-card {` with `padding: 24px`
    - App.css contains `.step-number {` with `background: var(--accent)`
    - App.css does NOT contain `linear-gradient` inside any `.step-number` rule
    - App.css contains `.song-table-card {` with `border-radius: 16px`
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>
    All 3 steps wrapped in monolith-card containers with solid-accent numbered circles.
    Step 2 inner table card has reduced 16px radius. No gradients. Builds clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase is CSS/JSX-only with a static local PNG asset. No user input is processed, no network calls are added, no authentication is involved.

| Boundary | Description |
|----------|-------------|
| None | No new trust boundaries — visual-only changes within existing client bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | hero-banner.png | accept | Static decorative image with no embedded metadata of concern. aria-hidden="true" prevents screen reader confusion. |

ASVS V2-V6 do not apply. No new inputs, no network calls, no auth, no session handling. V5 (input validation) not applicable — no inputs added.
</threat_model>

<verification>
1. `cd client && npm run build` — builds without TypeScript or Vite errors
2. Visual: hero banner renders below support-banner, above step cards
3. Visual: support-banner is visually identical to before (D-05)
4. Visual: all 3 steps show numbered circles and card containers
5. Visual: toggle dark/light mode — banner and cards adapt (D-06)
6. Inspect step-number element — `background` is `var(--accent)`, no gradient (D-04)
</verification>

<success_criteria>
- Hero banner image displays at full-width, ~220px height, with title + subtitle overlaid (D-01, D-02)
- Support Server Costs banner is visually unchanged (D-05)
- All 3 step sections wrapped in monolith-card with numbered step indicators (D-03, D-04)
- Step numbers use solid var(--accent) background, no gradients (D-04)
- Theme toggle works — banner text stays white, cards switch with theme (D-06)
- `npm run build` passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-premium-title-banner-and-modern-step-cards/06-01-SUMMARY.md`
</output>
