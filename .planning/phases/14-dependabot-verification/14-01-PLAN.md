# Plan: 14-01 — Dependabot Verification

**Phase:** 14 — Dependabot Verification  
**Plan:** 14-01  
**Goal:** Confirm PRs #2 and #3 closed; resolve 1 remaining npm audit vulnerability (ip-address XSS)  
**Requirements:** SEC-01, SEC-02  
**Estimated complexity:** Trivial (lockfile-only change)

---

## Pre-flight

**Current state (verified 2026-05-12):**
- PR #2 (`bump npm_and_yarn group 6 updates`): CLOSED ✅
- PR #3 (`bump npm_and_yarn group dev`): CLOSED ✅
- `npm audit`: 1 moderate — `ip-address <=10.1.0` (XSS in Address6 HTML methods)
  - Chain: `puppeteer → @puppeteer/browsers → proxy-agent → socks-proxy-agent → socks → ip-address`
  - Fix: `npm audit fix` bumps `ip-address 10.1.0 → 10.2.0` (lockfile-only, no direct dep change)

---

## Tasks

### Task 1 — Document PR closure (SEC-01)

Confirm both PRs are closed in the security record. No action needed — state already verified.

```bash
gh pr view 2 --json state,title
gh pr view 3 --json state,title
```

Expected: `"state":"CLOSED"` for both.

---

### Task 2 — Fix ip-address vulnerability (SEC-02)

```bash
npm audit fix
```

Expected: `ip-address` bumped to `10.2.0` in `package-lock.json`. No `node_modules` restructuring.

---

### Task 3 — Verify clean audit

```bash
npm audit
```

Expected exit code 0 with zero vulnerabilities.

---

### Task 4 — Commit lockfile change

```bash
git add package-lock.json
git commit -m "fix(sec): bump ip-address 10.1.0→10.2.0 via npm audit fix (GHSA-v2v4-37r5-5v8g)"
```

---

## Threat Model

| ID | Threat | Mitigation |
|----|--------|------------|
| T-14-01 | `npm audit fix` may pull semver-major upgrade | Dry-run confirmed: only ip-address patch bump. No `--force` needed. |
| T-14-02 | Lockfile change breaks Replit install | ip-address is transitive dev-only (puppeteer). No prod bundle change. |

---

## Success Criteria

- [ ] `gh pr view 2` → `CLOSED`
- [ ] `gh pr view 3` → `CLOSED`
- [ ] `npm audit` exits 0 after fix
- [ ] Lockfile committed to git

---

## Files Modified

- `package-lock.json` — lockfile only (ip-address 10.1.0 → 10.2.0)

---

## Notes

- XSS in `ip-address` is exploitable only via HTML-emitting methods on Address6 objects — not directly reachable from this app's request path (puppeteer uses it for proxy resolution). Still worth closing.
- PRs were closed (not merged) because Phase 8 applied fixes directly via `npm audit fix --force` — Dependabot PRs became stale.
