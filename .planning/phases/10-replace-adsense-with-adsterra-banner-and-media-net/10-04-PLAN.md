---
phase: 10-replace-adsense-with-adsterra-banner-and-media-net
plan: 04
type: execute
wave: 1
depends_on: [10-01]
files_modified:
  - client/index.html
  - public/index.html
autonomous: true
requirements: [ADM-04, ADM-08]
tags: [head-script, sovrn, adsterra, dual-file-sync]

must_haves:
  truths:
    - "client/index.html <head> contains the Sovrn signal <script async> tag immediately after the existing AdSense script"
    - "public/index.html <head> contains the identical Sovrn signal <script async> tag (synced from client/)"
    - "Neither client/index.html nor public/index.html contains a head-level Adsterra invoke.js <script> tag (per CONTEXT lock)"
    - "AdSense script tag (already present) is preserved verbatim — kept inert for future reapply"
    - "No CSP/CORS headers added to server.js (no Helmet middleware)"
  artifacts:
    - path: "client/index.html"
      provides: "Vite source HTML with AdSense (inert) + Sovrn signal head scripts"
      contains: "ad.lijit.com/www/sovrn_signal/sovrn_signal.js"
    - path: "public/index.html"
      provides: "Deploy mirror — identical head section synced from client/"
      contains: "ad.lijit.com/www/sovrn_signal/sovrn_signal.js"
  key_links:
    - from: "client/index.html"
      to: "public/index.html"
      via: "Manual head sync (Phase 9 dual-file pattern); deploy.sh cp -r client/dist/* public/ regenerates on next build"
      pattern: "ad\\.lijit\\.com"
    - from: "public/index.html (head)"
      to: "Sovrn Commerce platform"
      via: "<script async src='//ad.lijit.com/www/sovrn_signal/sovrn_signal.js?iid=...'> auto-rewrites outbound merchant <a href> attributes at runtime"
      pattern: "sovrn_signal\\.js\\?iid="
---

<objective>
Add the Sovrn Commerce affiliate-link rewriter `<script async>` tag to BOTH `client/index.html` (Vite source) AND `public/index.html` (deploy mirror) — exactly the dual-file head-sync pattern Phase 9 used for SEO meta tags. The Sovrn signal is the ONLY new head-level script this phase adds. Adsterra's invoke.js is explicitly NOT a head script — it lives inside AdSlot.tsx's useEffect (locked CONTEXT decision per Finding §3).

Purpose:
- ADM-04: Adsterra script wired with no CSP/CORS regression — confirmed by NOT adding Helmet/CSP middleware to server.js (audit-only).
- ADM-08: Sovrn signal head-level script tag — global outbound-link rewriter, runs once per page load, auto-tags every outbound merchant `<a href>`.

Output:
- 2 modified files (client/index.html + public/index.html), 0 new files, 0 npm installs.
- Zero edits to server.js (Helmet absence verified).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-CONTEXT.md
@.planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-RESEARCH.md
@.planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-PATTERNS.md
@.planning/research/ad-networks-vs-adsense.md

@client/index.html
@public/index.html
@server.js

<interfaces>
<!-- Existing client/index.html lines 47-48 (the head-script analog to extend) -->
<script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-2601322490070593"
     crossorigin="anonymous"></script>

<!-- Existing public/index.html lines 47-48 (identical to client/index.html — Phase 9 dual-file sync) -->
<script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-2601322490070593"
     crossorigin="anonymous"></script>

<!-- VITE_SOVRN_SITE_ID value from client/.env (populated in plan 01 task 2) -->
<!-- The placeholder YOUR_SOVRN_SITE_ID below MUST be substituted with the real value -->

<!-- server.js head-section verification: explicit-route block at lines 100-160 (no Helmet anywhere) -->
for (const file of ['ads.txt', 'robots.txt', 'sitemap.xml']) { ... }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Sovrn signal script to client/index.html and public/index.html (dual-file head sync)</name>
  <read_first>
    - client/index.html (the Vite source — lines 47-48 contain the AdSense script analog)
    - public/index.html (the deploy mirror — identical head section)
    - .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-CONTEXT.md (Script injection sites — LOCKED — confirms Sovrn IS in head, Adsterra is NOT)
    - .planning/research/ad-networks-vs-adsense.md §9.2 + Example 2 (head additions)
    - .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-PATTERNS.md ("client/index.html" + "public/index.html" sections)
    - .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-RESEARCH.md "Anti-Patterns to Avoid" — first bullet: edit BOTH files, not just public/
    - client/.env (plan 01 task 5) — VITE_SOVRN_SITE_ID value
  </read_first>
  <action>
    **Step 1 — Capture VITE_SOVRN_SITE_ID** from `client/.env`. If user deferred Sovrn registration in plan 01 (resume signal "deferred"):
    - Skip this entire task and document in 10-04-SUMMARY.md that ADM-08 is partially deferred (ads.txt lines also omitted in plan 02; head script omitted here). Phase still ships the Adsterra-only path.
    - Wave 3 verification will skip Sovrn checks accordingly.
    - **Deferral consequences (must be recorded in 10-06-SUMMARY.md):** ADM-08 status in `.planning/REQUIREMENTS.md` remains unchecked (`- [ ] **ADM-08**`); 10-06-SUMMARY.md ADM closure table marks ADM-08 = `deferred` (NOT `closed`). Phase 10 can still ship if the user explicitly accepts ADM-08 deferral — record the acceptance in 10-06-SUMMARY.md UAT step 7 notes. Otherwise (user retroactively wants Sovrn), re-run plan 01 task 2 and revisit this plan + plan 02 task 1.

    Otherwise proceed with Step 2.

    **Step 2 — Patch `client/index.html`.** Locate the AdSense `<script async>` line at line 47-48:

    ```html
    <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-2601322490070593"
         crossorigin="anonymous"></script>
    ```

    Insert TWO new lines IMMEDIATELY AFTER the AdSense script's closing `</script>` tag and BEFORE the closing `</head>` tag (line 49). Substitute `{VITE_SOVRN_SITE_ID}` with the real value from client/.env. Final block:

    ```html
        <!-- AdSense (kept inert — currently rejected; will resume on reapply) -->
        <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-2601322490070593"
             crossorigin="anonymous"></script>

        <!-- Phase 10 ADM-08: Sovrn Commerce affiliate-link rewriter (auto-tags outbound merchant <a href>) -->
        <script async src="//ad.lijit.com/www/sovrn_signal/sovrn_signal.js?iid={VITE_SOVRN_SITE_ID}"></script>
      </head>
    ```

    **Critical constraints:**
    - The AdSense `<script async>` line is LOAD-BEARING — preserve it byte-for-byte. Do not change `client=ca-pub-2601322490070593`. Do not remove `crossorigin="anonymous"`. Adding the inline HTML comment `<!-- AdSense (kept inert ...) -->` immediately above it is allowed and recommended for clarity (matches PATTERNS.md).
    - The Sovrn URL MUST start with `//` (protocol-relative) to match the protocol of the host page (https in production, http in dev). Do NOT use `https://` only.
    - The Sovrn `?iid={VITE_SOVRN_SITE_ID}` query parameter MUST contain the real Sovrn Site ID — substitute the literal value, do not leave the `{...}` placeholder in committed code.
    - Do NOT add an Adsterra invoke.js head-level script tag — this is FORBIDDEN per CONTEXT.md `<decisions>` "Script injection sites" lock. invoke.js is injected by AdSlot.tsx's useEffect (Wave 1 plan 03 task 1).

    **Step 3 — Mirror the same edit into `public/index.html`.** Apply byte-for-byte identical changes. The two files MUST have an identical head section after this task — Phase 9 RESEARCH §A3 documented that `deploy.sh cp -r client/dist/* public/` will eventually overwrite `public/index.html` from the build, so the manual mirror is for immediate Replit pickup before next build.

    **Step 4 — Audit for forbidden additions.** Run:
    ```bash
    grep -E "highperformanceformat" client/index.html public/index.html
    ```
    MUST return empty. If it matches, an executor accidentally added Adsterra invoke.js to the head — REMOVE it immediately. invoke.js belongs ONLY in AdSlot.tsx.

    **Step 5 — Audit server.js for Helmet/CSP middleware** (verify-only — must NOT be present):
    ```bash
    grep -cE "helmet|Content-Security-Policy" server.js
    ```
    MUST return 0. If a previous unrelated commit added Helmet, that breaks Adsterra/Sovrn — escalate to user. (Per RESEARCH §3.1 + Architectural Responsibility Map: server.js has zero CSP and Phase 10 must NOT add it.)

    Do NOT modify `server.js` or any other file. Do NOT install any npm package.
  </action>
  <verify>
    <automated>
      if grep -qE "^VITE_SOVRN_SITE_ID=.+" client/.env 2>/dev/null; then grep -c "sovrn_signal.js" client/index.html | grep -q "^1$"; else grep -c "sovrn_signal.js" client/index.html | grep -q "^0$"; fi
      if grep -qE "^VITE_SOVRN_SITE_ID=.+" client/.env 2>/dev/null; then grep -c "sovrn_signal.js" public/index.html | grep -q "^1$"; else grep -c "sovrn_signal.js" public/index.html | grep -q "^0$"; fi
      grep -c "adsbygoogle.js?client=ca-pub-2601322490070593" client/index.html | grep -q "^1$"
      grep -c "adsbygoogle.js?client=ca-pub-2601322490070593" public/index.html | grep -q "^1$"
      ! grep -E "highperformanceformat" client/index.html
      ! grep -E "highperformanceformat" public/index.html
      ! grep -E "iid=\{" client/index.html
      ! grep -E "iid=\{" public/index.html
      grep -cE "helmet|Content-Security-Policy" server.js | grep -q "^0$"
      cd client && npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - `client/index.html` contains exactly one `ad.lijit.com/www/sovrn_signal/sovrn_signal.js?iid=...` line in the `<head>`
    - `public/index.html` contains exactly one `ad.lijit.com/www/sovrn_signal/sovrn_signal.js?iid=...` line in the `<head>`
    - The `?iid=` value in BOTH files is a real Sovrn Site ID, NOT the literal `{VITE_SOVRN_SITE_ID}` placeholder
    - The AdSense script tag (`adsbygoogle.js?client=ca-pub-2601322490070593`) is preserved in both files (still exactly 1 occurrence each)
    - `grep highperformanceformat client/index.html public/index.html` returns nothing (Adsterra invoke.js NOT in head)
    - `grep -cE "helmet|Content-Security-Policy" server.js` returns 0 (no CSP regression)
    - `cd client && npm run build` exits 0 — Vite picks up the modified index.html as its entry
    - Diff: only `<head>` section between AdSense script and `</head>` is modified in both files; nothing else (verify with `git diff client/index.html public/index.html` — no body or other-tag changes)
  </acceptance_criteria>
  <done>
    Both index.html files have identical head sections containing AdSense (preserved inert) + Sovrn (new) — total of 2 head-level `<script async>` tags. Adsterra invoke.js is NOT in head (will be injected by AdSlot at runtime). server.js untouched.
  </done>

  <threat_model>
    **T-10-csp** (STRIDE: Tampering / Denial — CSP regression blocks third-party scripts):
    - Disposition: mitigate
    - Mitigation: explicit audit step verifies `grep -cE "helmet|Content-Security-Policy" server.js` returns 0. server.js has had no Helmet middleware historically (verified via Phase 8 + Phase 9 audits). If a future phase adds Helmet, it must whitelist `highperformanceformat.com`, `ad.lijit.com`, `cdn.iubenda.com`, and `pagead2.googlesyndication.com` in script-src.
    - Severity: MEDIUM
  </threat_model>
</task>

</tasks>

<verification>
After this single-task plan completes:
- `diff <(grep -A0 "<head>" client/index.html | head -50) <(grep -A0 "<head>" public/index.html | head -50)` should show effectively identical head sections (modulo whitespace + the additional Vite hashed JS module preload that public/index.html has — that's normal post-build state)
- `cd client && npm run build && npm run preview` boots without errors
- Browser DevTools Network tab on `http://localhost:4173/` shows requests for `adsbygoogle.js` (AdSense, will 200 with no-op since rejected) and `sovrn_signal.js` (Sovrn, will 200 if Site ID is valid)
</verification>

<success_criteria>
1. Both client/index.html and public/index.html have the Sovrn signal `<script async>` tag with a real Site ID
2. AdSense script tag preserved verbatim in both files
3. NO Adsterra invoke.js head-level tag in either file (Adsterra is injected by AdSlot at runtime)
4. server.js has zero Helmet/CSP middleware (audit pass)
5. Build still works
</success_criteria>

<output>
After completion, create `.planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-04-SUMMARY.md` documenting:
- Sovrn Site ID substituted (real / deferred)
- Files modified: 2 (client/index.html, public/index.html)
- AdSense script preserved (yes — verbatim)
- Adsterra invoke.js head-tag absent (confirmed)
- Helmet/CSP audit (server.js — zero matches)
</output>
