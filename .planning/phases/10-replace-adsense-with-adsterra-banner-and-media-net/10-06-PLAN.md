---
phase: 10-replace-adsense-with-adsterra-banner-and-media-net
plan: 06
type: execute
wave: 3
depends_on: [10-02, 10-05]
files_modified: []
autonomous: false
requirements: [ADM-01, ADM-02, ADM-03, ADM-04, ADM-05, ADM-06, ADM-08, ADM-09]
tags: [verification, deploy, lighthouse, uat, manual]

must_haves:
  truths:
    - "https://sunozip.com/ads.txt returns the dual/quad-entry file with all configured DIRECT lines"
    - "https://sunozip.com/privacy returns 200 and renders the Iubenda policy inside the .monolith-card"
    - "https://sunozip.com/sitemap.xml lists /privacy"
    - "Lighthouse mobile run on https://sunozip.com reports CLS < 0.1 and zero CSP violations"
    - "Adsterra publisher dashboard shows sunozip.com status = 'Approved' within 60 minutes of deploy"
    - "Banner renders above the existing footer with 'Advertisement' label visible"
    - "Incognito interaction with https://sunozip.com for 60s produces zero popunder/redirect/social-bar events"
    - "Golden-path download flow (paste URL → fetch → download ZIP) completes with ID3 tags embedded — no functional regression"
    - "Theme toggle still works dark↔light"
    - "Phase 7 donation modal still triggers on 1st + 5th download"
  artifacts: []
  key_links:
    - from: "deploy.sh"
      to: "https://sunozip.com"
      via: "git push triggers Replit pull + restart; public/ deploy mirror serves new ads.txt + index.html + sitemap.xml"
      pattern: "git push"
    - from: "Adsterra dashboard"
      to: "https://sunozip.com/ads.txt"
      via: "Hourly poll matching the publisher ID + tag hash from plan 02"
      pattern: "adsterra"
---

<objective>
Final wave: build, deploy via `deploy.sh`, run automated curl + Lighthouse checks, then manual incognito UAT to confirm no functional regression and no popunder fires. This wave is autonomous: false because it requires real production deploy + human-only Adsterra dashboard polling + manual download-flow UAT.

Purpose: Close ADM-01..09 by exercising the full integration on https://sunozip.com (not localhost) — Adsterra polls live URLs, Lighthouse mobile reports against production, popunder behavior only manifests on the deployed site.

Output:
- 0 file changes (verification-only).
- A 10-06-SUMMARY.md documenting: deploy SHA, curl outputs, Lighthouse CLS score, Adsterra dashboard status, UAT regression result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-CONTEXT.md
@.planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-VALIDATION.md
@.planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-RESEARCH.md
@deploy.sh
@.replit
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build, deploy, and run automated curl + Lighthouse verification</name>
  <read_first>
    - deploy.sh (the script being run)
    - .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-VALIDATION.md "Per-Task Verification Map" + "Phase Requirements → Test Map"
    - .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-RESEARCH.md "Common Pitfalls" + "Validation Architecture"
    - public/ads.txt (post-plan-02; the file deploy.sh will git-push)
    - public/sitemap.xml (post-plan-02)
    - public/index.html (post-plan-04 head sync)
  </read_first>
  <action>
    **Step 1 — Pre-flight audit.** Confirm Wave 1 + Wave 2 outputs are committed:
    ```bash
    git status
    ```
    Should show all of: `client/src/App.tsx`, `client/src/components/AdSlot.tsx`, `client/src/pages/Privacy.tsx`, `client/.env.example`, `client/index.html`, `public/index.html`, `public/ads.txt`, `public/sitemap.xml`, `.planning/REQUIREMENTS.md`, `.planning/phases/10-.../MEDIA-NET-SUBMISSION.md` either staged or already in HEAD.

    Confirm `client/.env` is NOT in git status (still gitignored).

    **Step 2 — Build the client.**
    ```bash
    cd client && npm run build
    ```
    Must exit 0. The fresh dist contains the post-Vite-build `index.html` referencing the hashed JS module (e.g., `/assets/index-XXXXX.js`).

    **Step 3 — Run deploy.sh.**
    ```bash
    cd /Users/ashley.rhodes/proj/scripts/learning-projects/suno-playlist-downloader && ./deploy.sh
    ```
    The script does: `npm run build` (again — idempotent), `rm -rf public/assets`, `cp -r client/dist/* public/`, `git add public/`, `git commit -m "build: rebuild public/ for deployment"`, `git push`. Replit pulls + restarts on push (per `.replit` deployment config).

    NOTE: deploy.sh's `cp -r client/dist/* public/` will OVERWRITE `public/index.html` with the freshly-built version. Plan 04 manually mirrored the head changes into `public/index.html` for immediate effect — but the build also propagates the same head changes from `client/index.html` into the new `public/index.html`. The two are equivalent post-build. Verify with:
    ```bash
    grep "ad.lijit.com/www/sovrn_signal" public/index.html
    ```
    Must return one line. (If empty, the build failed to propagate — re-run `cd client && npm run build` then `cp -r client/dist/* public/`.)

    **Step 4 — Wait 30-60 seconds for Replit to pull + restart.**

    **Step 5 — Automated curl verifications.** Run the following commands in order. Each must produce the documented output.

    **5a. ads.txt:**
    ```bash
    curl -s https://sunozip.com/ads.txt | grep -cE "google|adsterra|lijit|sovrn"
    ```
    - Expected: 4 lines (full Sovrn integration) OR 2 lines (Sovrn deferred — google + adsterra only).

    **5b. /privacy returns 200:**
    ```bash
    curl -s -o /dev/null -w "%{http_code}\n" https://sunozip.com/privacy
    ```
    - Expected: `200`

    **5c. /privacy contains Iubenda script reference (loaded via Privacy.tsx useEffect — this verifies the bundle includes Privacy.tsx):**
    ```bash
    curl -s https://sunozip.com/privacy | grep -E "iubenda" | head -1
    ```
    - Expected: at least one line referencing iubenda (either `cdn.iubenda.com` from the script tag injected on mount, or the policy URL hardcoded in Privacy.tsx — note: this only matches AFTER the script has injected, so if testing immediately post-deploy it may return empty until first browser load).
    - Acceptable fallback: `curl -s https://sunozip.com/ | grep -c iubenda` returns 0 (Iubenda must NOT be on home page); `curl -s https://sunozip.com/privacy | grep -c "Privacy Policy"` returns at least 1 (the H1 + anchor text).

    **5d. /sitemap.xml lists /privacy:**
    ```bash
    curl -s https://sunozip.com/sitemap.xml | grep -c "/privacy"
    ```
    - Expected: ≥1

    **5e. /robots.txt does not disallow /privacy:**
    ```bash
    curl -s https://sunozip.com/robots.txt | grep -E "Disallow.*privacy"
    ```
    - Expected: empty (no match)

    **5f. Home page bundles AdSlot's invoke.js loader URL** (the URL string survives Vite minification; the `minHeight: 90` numeric inline-style does NOT — Vite compresses numeric style values):
    ```bash
    BUNDLE=$(curl -s https://sunozip.com/ | grep -oE '/assets/index-[A-Za-z0-9_-]+\.js' | head -1) && curl -s "https://sunozip.com${BUNDLE}" | grep -c 'highperformanceformat'
    ```
    - Expected: ≥1 (URL-fragment survives minification because it is a string literal, not a numeric style value).
    - Why this assertion: confirms AdSlot.tsx (plan 03) was bundled into the production JS — without the `highperformanceformat` URL fragment present, the Adsterra invoke.js loader would never fire client-side. This is the most reliable bundle-presence signal for AdSlot.

    **5g. Lighthouse mobile CLS check.**
    ```bash
    npx --yes lighthouse https://sunozip.com --form-factor=mobile --only-categories=performance --output=json --output-path=./lh-report.json --chrome-flags="--headless"
    cat ./lh-report.json | python3 -c "import json,sys; d=json.load(sys.stdin); cls=d['audits']['cumulative-layout-shift']['numericValue']; print(f'CLS: {cls}'); sys.exit(0 if cls < 0.1 else 1)"
    ```
    - Expected: `CLS: 0.0X` where X is between 0 and 0.099, command exits 0.
    - If CLS ≥ 0.1: investigate AdSlot wrapper — `min-height` may not be reserving. Check AdSlot.tsx wasn't accidentally modified post-plan-03.

    **5h. Helmet/CSP regression audit on live response headers:**
    ```bash
    curl -sI https://sunozip.com/ | grep -iE "content-security-policy|x-content"
    ```
    - Expected: empty (no CSP header set) — confirms ADM-04 no CSP regression.

    **Step 6 — Document results.** Capture all curl outputs, the Lighthouse CLS score, and the deploy commit SHA in the SUMMARY artifact (next task).
  </action>
  <verify>
    <automated>
      curl -s https://sunozip.com/ads.txt | grep -cE "google|adsterra" | awk '$1>=2'
      curl -s -o /dev/null -w "%{http_code}" https://sunozip.com/privacy | grep -q "^200$"
      curl -s https://sunozip.com/sitemap.xml | grep -c "/privacy" | awk '$1>=1'
      ! curl -s https://sunozip.com/robots.txt | grep -E "Disallow.*privacy"
      ! curl -sI https://sunozip.com/ | grep -iE "content-security-policy"
    </automated>
  </verify>
  <acceptance_criteria>
    - `git push` succeeded (deploy.sh exits 0)
    - `https://sunozip.com/ads.txt` returns at least 2 DIRECT lines (Google + Adsterra) — 4 if Sovrn integrated
    - `https://sunozip.com/privacy` returns HTTP 200
    - `https://sunozip.com/sitemap.xml` lists `/privacy`
    - `https://sunozip.com/robots.txt` has no Disallow for `/privacy`
    - Lighthouse mobile CLS < 0.1 (logged in summary)
    - No `Content-Security-Policy` header in response
    - Bundle JS contains the string `highperformanceformat` (AdSlot's invoke.js URL fragment survived bundling/minification — this is the reliable signal that AdSlot.tsx is in the production bundle)
  </acceptance_criteria>
  <done>
    Production deploy verified: ads.txt + sitemap + privacy route serving correctly, no CSP regression, CLS <0.1.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual UAT — golden-path download flow + theme toggle + donation modal regression</name>
  <read_first>
    - .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-VALIDATION.md "Manual-Only Verifications" table
    - .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-CONTEXT.md (Functional regression guard — LOCKED)
    - ROADMAP.md Phase 7 success criteria (donation modal trigger logic)
    - client/src/App.tsx lines 70-90 (the canonical Phase 7 download-counter implementation; verifies the localStorage key is `suno-download-count`)
    - client/src/components/DonationModal.tsx (the modal itself; props-only, no localStorage access)
  </read_first>
  <what-built>
    Live deploy from Task 1. The user is the only oracle for end-to-end download UAT (real Suno playlist URL + observing ZIP contents).
  </what-built>
  <how-to-verify>
    Open https://sunozip.com in **Chrome incognito** with NO browser extensions (especially no ad-blockers — they would mask Adsterra script load):

    **UAT Step 1 — Visual placement check (ADM-05):**
    1. Scroll to bottom of home page.
    2. Verify a 728x90 area appears IMMEDIATELY ABOVE the existing footer.
    3. Verify a small "Advertisement" label in 11px muted-grey sits centered immediately above that area.
    4. Verify the area has a subtle 1px border with rounded 12px corners and transparent background (page bg shows through).
    5. If `VITE_ADSTERRA_UNIT_KEY` is set: a banner ad (advertiser content) renders inside. If not set: the area is empty (silent — this is the locked graceful-degradation behavior).

    **UAT Step 2 — Privacy route check (ADM-06):**
    1. In the footer, click "Privacy Policy" link.
    2. Browser navigates to https://sunozip.com/privacy (full page reload).
    3. Verify the page renders:
       - "← Back to downloader" link at top (links to `/`)
       - "Privacy Policy" H1
       - A `.monolith-card`-styled box containing the Iubenda-rendered policy text (NOT a 404, NOT just the anchor text "Privacy Policy" alone — Iubenda's script must transform the anchor into rendered HTML).
    4. Verify dark theme matches the home page (uses CSS variables — automatic).
    5. Click "← Back to downloader" → returns to home.

    **UAT Step 3 — No popunder check (ADM-09 + Pitfall 7):**
    1. Back on home page in incognito Chrome with no extensions.
    2. Click around the page for 60 seconds: paste a URL into the input, click "Get Playlist Songs", scroll, click theme toggle.
    3. Confirm: NO new tabs/windows open. NO full-page redirects. NO overlay social-bar elements appear at top/bottom of viewport.
    4. If a popunder fires: Adsterra dashboard formats are misconfigured — return to plan 01 task 1 and disable popunder/social-bar/in-page-push.

    **UAT Step 4 — Golden-path download flow (ADM-09):**
    1. Paste a known Suno playlist URL (e.g., a public `https://suno.com/playlist/{id}` URL).
    2. Click "Get Playlist Songs" → wait for fetch to complete; song table populates.
    3. Click "Download as ZIP" → wait for download.
    4. Open the downloaded ZIP. Verify:
       - Filename matches existing convention (was unchanged by Phase 10).
       - Contains MP3 files with embedded ID3 tags (title, track number, cover art) — this is the existing Phase 1-3 ID3 pipeline.
    5. NO console errors in DevTools during the full flow.

    **UAT Step 5 — Theme toggle (ADM-09):**
    1. Click theme toggle icon → confirm dark↔light cross-fade transition with no flash of unstyled content.
    2. Confirm AdSlot border ring + label color adapt to the new theme automatically (CSS variables resolve).
    3. Confirm `/privacy` page also adapts to the new theme.

    **UAT Step 6 — Donation modal trigger (Phase 7 regression — ADM-09):**

    Phase 7 stores the cumulative download count in `localStorage` under the key `suno-download-count` (verified by reading `client/src/App.tsx` lines 70-79 during read_first). The trigger logic at App.tsx line 76 is `if (next === 1 || next % 5 === 0)` — modal opens on the 1st, 5th, 10th, 15th... downloads.

    1. Open DevTools Console. Note current counter: `localStorage.getItem('suno-download-count')`.
    2. Reset to 0: `localStorage.setItem('suno-download-count', '0')`.
    3. Trigger 1 download → modal SHOULD appear (1st download; `next === 1`).
    4. Close modal, trigger 3 more downloads (2nd, 3rd, 4th) → modal should NOT appear.
    5. Trigger the 5th download → modal SHOULD appear (`next % 5 === 0`).
    6. After UAT, verify `localStorage.getItem('suno-download-count')` reads `"5"` (string, not number).

    **UAT Step 7 — Adsterra dashboard "Approved" status (ADM-01, ADM-03):**
    1. Wait at least 60 minutes after deploy for Adsterra's hourly ads.txt poll.
    2. Log in to Adsterra publisher dashboard.
    3. Navigate to Websites → sunozip.com.
    4. Verify status shows "Approved" or "Active" (not "Pending" or "Verification failed").
    5. Verify revenue stats begin tracking impressions (may show 0 initially — what matters is the dashboard is live, not zero-state).

    **UAT Step 8 — DevTools network audit (ADM-04, ADM-08):**
    1. Open DevTools → Network tab → filter by `highperformanceformat`. Click around home page. Should see one 200 response for the Adsterra invoke.js (only if VITE_ADSTERRA_UNIT_KEY is set).
    2. Filter by `sovrn_signal`. Should see one 200 response for `sovrn_signal.js` on home page (only if VITE_SOVRN_SITE_ID is set).
    3. Console tab: zero CSP violations expected.
    4. Filter by `iubenda`. Should see ZERO requests on home page; should see at least one (cdn.iubenda.com) on `/privacy` page only.
  </how-to-verify>
  <acceptance_criteria>
    - UAT 1: Banner area + label visible above footer with correct styling
    - UAT 2: /privacy returns 200, renders Iubenda policy inside .monolith-card, dark theme intact, back-link returns to home
    - UAT 3: Zero popunders / redirects / social-bars in 60s incognito interaction
    - UAT 4: Golden-path download produces a ZIP with ID3 tags; zero console errors
    - UAT 5: Theme toggle works on both home and /privacy
    - UAT 6: Donation modal triggers on 1st and 5th download (`next === 1 || next % 5 === 0` per App.tsx line 76); `suno-download-count` key updates correctly
    - UAT 7: Adsterra dashboard status = "Approved" within 60min of deploy
    - UAT 8: DevTools Network shows expected 200s for Adsterra (home), Sovrn (home), Iubenda (/privacy only); zero CSP violations in Console
  </acceptance_criteria>
  <resume-signal>
    Type "approved" if all 8 UAT steps pass. If any fail, describe the failing step (e.g., "UAT 3 failed — popunder fired on 2nd click; Adsterra dashboard re-check needed") and the orchestrator will route to gap-closure planning.
  </resume-signal>

  <threat_model>
    **T-10-formats** (revisited from plan 01 — runtime confirmation):
    - Disposition: mitigate (verified at UAT)
    - Mitigation: incognito 60s observation in UAT step 3 detects any popunder leakage from Adsterra dashboard misconfiguration. If detected, fail the gate and route back to plan 01 task 1.

    **T-10-iframe** (revisited from plan 03 — runtime confirmation):
    - Disposition: accept (with monitoring)
    - Mitigation: AdSlot wrapper border quarantine confirmed visually in UAT step 1. Advertiser creative content audited via DevTools network in UAT step 8.

    Severity: MEDIUM. None should block merge unless UAT 3 detects a popunder.
  </threat_model>
</task>

<task type="auto">
  <name>Task 3: Generate 10-06-SUMMARY.md with full verification record</name>
  <read_first>
    - Outputs of Task 1 (curl results, Lighthouse JSON, deploy SHA)
    - Outputs of Task 2 (UAT 1-8 results from human checkpoint resume signal)
  </read_first>
  <action>
    Create `.planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-06-SUMMARY.md` with the following structure (substitute real values):

    ```markdown
    # Phase 10 Plan 06 — Verification Summary

    **Wave:** 3 (verification)
    **Deploy SHA:** {git rev-parse HEAD output from deploy.sh}
    **Deploy time:** {timestamp}

    ## Automated Curl Results

    | Check | Result |
    |---|---|
    | `curl https://sunozip.com/ads.txt` line count | {N} |
    | `curl /privacy` HTTP status | 200 |
    | `curl /sitemap.xml` contains /privacy | yes |
    | `curl /robots.txt` allows /privacy | yes |
    | `curl -I /` Content-Security-Policy header | absent |
    | Bundle contains `highperformanceformat` URL fragment | present |

    ## Lighthouse Mobile

    | Metric | Value | Target |
    |---|---|---|
    | CLS | {0.0X} | < 0.1 |
    | Performance score | {N} | informational |

    ## Manual UAT Results

    | Step | Status | Notes |
    |---|---|---|
    | 1. Visual placement | {pass/fail} | |
    | 2. /privacy route | {pass/fail} | |
    | 3. No popunder | {pass/fail} | |
    | 4. Golden-path download | {pass/fail} | |
    | 5. Theme toggle | {pass/fail} | |
    | 6. Donation modal trigger | {pass/fail} | |
    | 7. Adsterra dashboard "Approved" | {pass/fail} | |
    | 8. DevTools network audit | {pass/fail} | |

    ## Requirement Closure

    | ID | Status | Verified by |
    |---|---|---|
    | ADM-01 | {closed/pending} | UAT 7 (Adsterra dashboard banner-only) |
    | ADM-02 | {closed} | Lighthouse CLS + bundle audit |
    | ADM-03 | {closed} | Curl ads.txt |
    | ADM-04 | {closed} | UAT 8 + curl -I |
    | ADM-05 | {closed} | UAT 1 |
    | ADM-06 | {closed} | UAT 2 + curl /sitemap.xml |
    | ADM-07 | {closed in plan 01} | MEDIA-NET-SUBMISSION.md |
    | ADM-08 | {closed/deferred} | UAT 8 (Sovrn signal load) |
    | ADM-09 | {closed} | UAT 4-6 (no functional regression) |
    ```
  </action>
  <verify>
    <automated>
      test -f .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-06-SUMMARY.md
      grep -E "ADM-01|ADM-02|ADM-03|ADM-04|ADM-05|ADM-06|ADM-07|ADM-08|ADM-09" .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-06-SUMMARY.md | wc -l | awk '$1>=9'
      grep -E "Deploy SHA:" .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-06-SUMMARY.md
      grep -E "CLS" .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-06-SUMMARY.md
    </automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/10-.../10-06-SUMMARY.md` exists
    - Contains real Deploy SHA (not placeholder)
    - Contains real CLS value
    - Contains all 9 ADM-* IDs in the closure table
    - Contains UAT 1-8 results
  </acceptance_criteria>
  <done>
    SUMMARY artifact committed; phase ready for `/gsd-verify-work` review.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
- Production deploy live at https://sunozip.com
- All curl checks green
- Lighthouse CLS < 0.1
- Manual UAT 1-8 all pass
- Adsterra dashboard "Approved"
- 10-06-SUMMARY.md committed with full evidence trail
</verification>

<success_criteria>
1. deploy.sh exits 0; Replit picks up new public/ files
2. All 9 ADM-* requirements have verified status (closed or documented-deferred for Sovrn/Media.net if user opted out)
3. Zero functional regression in download flow / theme / donation modal
4. CLS < 0.1
5. SUMMARY artifact records the full verification trail for `/gsd-verify-work` consumption
</success_criteria>

<output>
After completion, the phase is ready for `/gsd-verify-work 10`. The 10-06-SUMMARY.md is the single source of truth for the verifier — it should be able to confirm phase closure from this file alone.
</output>
