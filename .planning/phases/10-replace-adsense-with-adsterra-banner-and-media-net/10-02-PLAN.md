---
phase: 10-replace-adsense-with-adsterra-banner-and-media-net
plan: 02
type: execute
wave: 1
depends_on: [10-01]
files_modified:
  - public/ads.txt
  - public/sitemap.xml
  - public/robots.txt
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [ADM-03, ADM-06, ADM-08]

must_haves:
  truths:
    - "https://sunozip.com/ads.txt returns four DIRECT lines (Google + Adsterra + Sovrn lijit + Sovrn sovrn) post-deploy"
    - "https://sunozip.com/sitemap.xml lists /privacy as a crawlable URL"
    - "https://sunozip.com/robots.txt does not Disallow /privacy"
    - "REQUIREMENTS.md has ADM-01..09 entries with status checkboxes"
    - "find . -name ads.txt (excluding node_modules + .git) returns exactly one path: ./public/ads.txt"
  artifacts:
    - path: "public/ads.txt"
      provides: "Publisher manifest for Adsterra hourly poll + Sovrn validation + AdSense reapply path"
      contains: "adsterra.com,"
    - path: "public/sitemap.xml"
      provides: "SEO crawl index including /privacy"
      contains: "<loc>https://sunozip.com/privacy</loc>"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase-10 requirement IDs ADM-01..09 backfilled for traceability"
      contains: "ADM-01"
  key_links:
    - from: "public/ads.txt"
      to: "Adsterra dashboard"
      via: "https://sunozip.com/ads.txt hourly poll matching publisher ID + tag hash"
      pattern: "adsterra.com, [^,]+, DIRECT, [a-f0-9]+"
    - from: "public/ads.txt"
      to: "Sovrn dashboard"
      via: "https://sunozip.com/ads.txt validation against lijit.com + sovrn.com lines"
      pattern: "(lijit|sovrn)\\.com, [^,]+, DIRECT, fafdf38b16bf6b2b"
    - from: "public/sitemap.xml"
      to: "Google Search Console"
      via: "Crawl discovery of /privacy URL for AdSense reapply page count"
      pattern: "<loc>https://sunozip\\.com/privacy</loc>"
---

<objective>
Patch the three public/ static config files (ads.txt, sitemap.xml, robots.txt) plus backfill ADM-01..09 into REQUIREMENTS.md. These are the static deploy artifacts already served by `server.js` lines 100-110 explicit routes — no Vite or React involvement, append-only edits.

Purpose:
- ADM-03: ads.txt dual-entry (AdSense kept inert + Adsterra DIRECT + Sovrn DIRECT × 2 lines) — Adsterra will not approve sunozip.com without seeing its DIRECT line in ads.txt within 60 minutes.
- ADM-06 (partial): sitemap.xml + robots.txt updates so Google indexes /privacy. Privacy.tsx is Wave 1 plan 03.
- ADM-08 (partial): Sovrn ads.txt lines (the script tag itself is plan 04). Sovrn validates ads.txt before activating affiliate revenue.
- Traceability: REQUIREMENTS.md currently has ADS-01..04 (Phase 4) but no ADM-* entries — Phase 9 RESEARCH §A1 noted this gap.

Output:
- 4 modified files, 0 new files, 0 npm installs.
- Single source of truth for ads.txt at `public/ads.txt` confirmed (no `client/public/ads.txt` created).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-CONTEXT.md
@.planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-RESEARCH.md
@.planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-PATTERNS.md
@.planning/research/ad-networks-vs-adsense.md
@public/ads.txt
@public/sitemap.xml
@public/robots.txt
@.planning/REQUIREMENTS.md
@server.js

<interfaces>
<!-- Existing public/ads.txt single line (the analog to extend) -->
google.com, pub-2601322490070593, DIRECT, f08c47fec0942fa0

<!-- Existing public/sitemap.xml structure (the template to append to) -->
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://sunozip.com/</loc>
    <lastmod>2026-04-14</lastmod>
    <changefreq>monthly</changefreq>
    <priority>1.0</priority>
  </url>
</urlset>

<!-- Existing public/robots.txt (verify-only — Allow: / covers /privacy) -->
User-agent: *
Allow: /
Disallow: /api/

Sitemap: https://sunozip.com/sitemap.xml

<!-- Existing REQUIREMENTS.md ID table convention (ADS-01..04 is the analog for ADM-01..09) -->
- [x] **ADS-01**: Google AdSense Auto Ads script tag present in index.html head with async loading
- [ ] **ADS-02**: ...

<!-- server.js explicit routes (NO change needed — verifies single-source-of-truth pattern) -->
for (const file of ['ads.txt', 'robots.txt', 'sitemap.xml']) {
  // serves from public/ directly
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch public/ads.txt with Adsterra DIRECT + Sovrn DIRECT × 2 lines (preserve AdSense)</name>
  <read_first>
    - public/ads.txt (the file being modified — current single Google line)
    - .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-CONTEXT.md (`ads.txt` LOCKED decision)
    - .planning/research/ad-networks-vs-adsense.md §9.1 (ads.txt example) + §A1 (path validation: `public/ads.txt`, NOT `client/public/`)
    - .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-PATTERNS.md ("public/ads.txt (publisher manifest)" section)
    - server.js lines 95-160 (confirms `app.get('/ads.txt')` serves from `public/`)
    - client/.env (created in plan 01) — values to substitute for placeholders
  </read_first>
  <action>
    **Step 1 — Audit single source of truth.** Run:
    ```bash
    find . -name ads.txt -not -path "*/node_modules/*" -not -path "*/.git/*"
    ```
    MUST return exactly `./public/ads.txt`. If a duplicate exists at `./client/public/ads.txt` or anywhere else, DELETE the duplicate before continuing (per CONTEXT.md Pitfall 1).

    **Step 2 — Read real values from `client/.env`** (populated in plan 01):
    - `VITE_ADSTERRA_PUB_ID` → substitutes `YOUR_ADSTERRA_PUB_ID`
    - `VITE_ADSTERRA_TAG_HASH` → substitutes `YOUR_ADSTERRA_TAG_HASH`
    - `VITE_SOVRN_PUB_ID` → substitutes `YOUR_SOVRN_PUB_ID` (in BOTH lijit.com and sovrn.com lines)

    **Step 3 — Write replacement content** to `public/ads.txt` (REPLACE the entire file contents):

    ```text
    # Google AdSense — kept inert pending reapply (currently rejected, harmless)
    google.com, pub-2601322490070593, DIRECT, f08c47fec0942fa0

    # Adsterra (Phase 10 primary display network) — values from Adsterra Account → ads.txt
    adsterra.com, {VITE_ADSTERRA_PUB_ID}, DIRECT, {VITE_ADSTERRA_TAG_HASH}

    # Sovrn Commerce (Phase 10 affiliate-link rewriter — both lijit.com + sovrn.com required per Sovrn KB)
    lijit.com, {VITE_SOVRN_PUB_ID}, DIRECT, fafdf38b16bf6b2b
    sovrn.com, {VITE_SOVRN_PUB_ID}, DIRECT, fafdf38b16bf6b2b
    ```

    Substitute the three `{...}` tokens with actual values from `client/.env`. The hash `fafdf38b16bf6b2b` is the Sovrn cert authority hash and is a literal constant per RESEARCH §9.1 (do NOT substitute).

    **Step 4 — If user opted out of Sovrn in plan 01 task 2** (resume signal was "deferred"): omit the two `lijit.com`/`sovrn.com` lines. Phase still ships ADM-03 + ADM-08 partial. Document the deferral in plan 02 SUMMARY.

    **Do NOT** create `client/public/ads.txt` — this is explicitly forbidden per CONTEXT.md (would create a duplicate that `deploy.sh` may overwrite). The single canonical path is `./public/ads.txt` per server.js explicit route.

    **Do NOT** add `helmet` middleware or modify `server.js` — out of scope per CLAUDE.md "no functional changes".
  </action>
  <verify>
    <automated>
      find . -name ads.txt -not -path "*/node_modules/*" -not -path "*/.git/*" | wc -l | grep -q "^[[:space:]]*1$"
      grep -E "^google\.com, pub-2601322490070593, DIRECT," public/ads.txt
      grep -E "^adsterra\.com, [^Y][^,]+, DIRECT, [^Y]" public/ads.txt
      grep -E "^lijit\.com, [^Y][^,]+, DIRECT, fafdf38b16bf6b2b$" public/ads.txt
      grep -E "^sovrn\.com, [^Y][^,]+, DIRECT, fafdf38b16bf6b2b$" public/ads.txt
    </automated>
  </verify>
  <acceptance_criteria>
    - `find . -name ads.txt -not -path "*/node_modules/*" -not -path "*/.git/*"` returns exactly one path: `./public/ads.txt`
    - `grep -c "DIRECT" public/ads.txt` returns 4 (Google + Adsterra + Sovrn × 2) — OR 2 if Sovrn deferred (Google + Adsterra)
    - `grep "^adsterra.com," public/ads.txt` returns one line with no `YOUR_` placeholder substring
    - `grep "fafdf38b16bf6b2b" public/ads.txt` returns 2 lines (lijit.com + sovrn.com) — OR 0 if Sovrn deferred
    - `grep "^google.com, pub-2601322490070593" public/ads.txt` still returns the original AdSense line (preserved, not deleted)
    - File ends with newline (POSIX-compliant)
  </acceptance_criteria>
  <done>
    public/ads.txt contains 4 DIRECT lines (or 2 if Sovrn deferred). All real publisher IDs substituted (no `YOUR_*` placeholders). AdSense line preserved verbatim.
  </done>

  <threat_model>
    **T-10-typo** (STRIDE: Spoofing — ads.txt forgery / typo squatting):
    - Disposition: mitigate
    - Mitigation: real Adsterra publisher ID + tag hash sourced from authenticated dashboard (plan 01 task 1); single-source-of-truth audit (`find` returns one path) prevents stale duplicates; Adsterra dashboard hourly poll detects typos within 60 min and surfaces "Approval pending" in dashboard.
    - Severity: MEDIUM
  </threat_model>
</task>

<task type="auto">
  <name>Task 2: Append /privacy URL to public/sitemap.xml; verify robots.txt allows it</name>
  <read_first>
    - public/sitemap.xml (the file being modified)
    - public/robots.txt (the file being audited — no edit expected)
    - .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-CONTEXT.md (Sitemap + robots.txt updates LOCKED)
    - .planning/research/ad-networks-vs-adsense.md Example 5 (sitemap patch) + Example 6 (robots audit)
    - .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-PATTERNS.md ("public/sitemap.xml" + "public/robots.txt" sections)
  </read_first>
  <action>
    **Step 1 — Patch public/sitemap.xml.** Insert a new `<url>` block immediately before `</urlset>`. The deploy date is today (2026-04-27); use that as `<lastmod>`. Final file content:

    ```xml
    <?xml version="1.0" encoding="UTF-8"?>
    <urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
      <url>
        <loc>https://sunozip.com/</loc>
        <lastmod>2026-04-14</lastmod>
        <changefreq>monthly</changefreq>
        <priority>1.0</priority>
      </url>
      <url>
        <loc>https://sunozip.com/privacy</loc>
        <lastmod>2026-04-27</lastmod>
        <changefreq>yearly</changefreq>
        <priority>0.3</priority>
      </url>
    </urlset>
    ```

    `changefreq=yearly` and `priority=0.3` are conventional for legal pages (low update cadence, low ranking weight) per PATTERNS.md.

    **Step 2 — Audit public/robots.txt** (no edit expected). Run:
    ```bash
    grep -E "^Disallow:.*privacy" public/robots.txt
    ```
    MUST return empty (no privacy disallow rule). The existing `Allow: /` covers `/privacy`. If the grep matches, escalate — that contradicts CONTEXT.md and requires user input.

    **Step 3 — Confirm `public/robots.txt` is unchanged from prior commit.** Run `git diff public/robots.txt` — should show no changes after this task. If somehow modified, revert.

    **Do NOT** edit `client/public/sitemap.xml` (does not exist; the canonical is `public/sitemap.xml` per server.js explicit route at line 144 in the for-loop).
  </action>
  <verify>
    <automated>
      grep -c "<loc>https://sunozip.com/privacy</loc>" public/sitemap.xml | grep -q "^1$"
      grep -c "<loc>https://sunozip.com/</loc>" public/sitemap.xml | grep -q "^1$"
      ! grep -E "^Disallow:.*privacy" public/robots.txt
      grep -c "<changefreq>yearly</changefreq>" public/sitemap.xml | grep -q "^1$"
      python3 -c "import xml.etree.ElementTree as ET; ET.parse('public/sitemap.xml')"
    </automated>
  </verify>
  <acceptance_criteria>
    - public/sitemap.xml contains exactly one `<loc>https://sunozip.com/privacy</loc>` line
    - public/sitemap.xml still contains the original `<loc>https://sunozip.com/</loc>` line (preserved)
    - public/sitemap.xml is valid XML — `python3 -c "import xml.etree.ElementTree as ET; ET.parse('public/sitemap.xml')"` exits 0
    - public/robots.txt has zero `Disallow:.*privacy` rules
    - `git diff public/robots.txt` shows no change (verify-only audit, no modification)
  </acceptance_criteria>
  <done>
    sitemap.xml has 2 `<url>` blocks (home + privacy). robots.txt unchanged. Both files served via existing server.js explicit routes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Backfill ADM-01..09 into .planning/REQUIREMENTS.md</name>
  <read_first>
    - .planning/REQUIREMENTS.md (current file — has ADS-01..04 in Phase 4 section, NO ADM-* entries)
    - .planning/ROADMAP.md lines 158-180 (Phase 10 ADM-01..09 source-of-truth definitions)
    - .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-PATTERNS.md (".planning/REQUIREMENTS.md (backfill ADM-01..09)" section — confirms tickbox + bold ID convention)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md`. Two changes required:

    **Change 1 — Add new section** `### Ad Monetisation` immediately AFTER the existing `### Donation Modal` section (currently at lines 53-58) and BEFORE the `## Future Requirements` heading (line 60). Use the exact tickbox + bold-ID convention:

    ```markdown
    ### Ad Monetisation

    - [ ] **ADM-01**: Adsterra publisher account approved with popunder/direct-link/social-bar formats DISABLED in dashboard
    - [ ] **ADM-02**: `<AdSlot>` React component (Mantine v6 + CSS-vars + min-height reservation) rendering banner without layout shift (CLS <0.1)
    - [ ] **ADM-03**: `public/ads.txt` updated as transitional dual-entry containing AdSense, Adsterra, and Sovrn publisher records, served at https://sunozip.com/ads.txt
    - [ ] **ADM-04**: Adsterra script tag wired with no CSP/CORS regressions on Replit/Cloud Run
    - [ ] **ADM-05**: First banner placement above footer, dark-mode friendly, "Advertisement" label visible per FTC
    - [ ] **ADM-06**: Privacy policy page added at /privacy (Iubenda standard embedding); robots.txt + sitemap.xml updated
    - [ ] **ADM-07**: Media.net publisher application submitted (logged with date and ticket ID)
    - [ ] **ADM-08**: Sovrn Commerce outbound-link rewriter integrated (one head script, ads.txt entries)
    - [ ] **ADM-09**: All download flows, settings, and API calls function identically — no functional regressions
    ```

    **Change 2 — Add 9 rows to the Traceability table** (currently ends at line 112 `| DON-06 | Phase 7 | Pending |`). Insert after the DON-06 row, before the Coverage block. Each row format:

    ```
    | ADM-01 | Phase 10 | Pending |
    | ADM-02 | Phase 10 | Pending |
    | ADM-03 | Phase 10 | Pending |
    | ADM-04 | Phase 10 | Pending |
    | ADM-05 | Phase 10 | Pending |
    | ADM-06 | Phase 10 | Pending |
    | ADM-07 | Phase 10 | Pending |
    | ADM-08 | Phase 10 | Pending |
    | ADM-09 | Phase 10 | Pending |
    ```

    **Change 3 — Update Coverage block** (line 114-117). Increment numbers:
    - `v2.0 requirements: 27 total` → `v2.0 requirements: 36 total`
    - `Mapped to phases: 27` → `Mapped to phases: 36`
    - Update `*Last updated: 2026-04-14 after Phase 7 planning*` → `*Last updated: 2026-04-27 after Phase 10 planning*`

    **Do NOT** modify any other section. Existing THME/CARD/TYPO/INTR/PLSH/ADS/DON entries must be preserved exactly.
  </action>
  <verify>
    <automated>
      grep -cE "^- \[ \] \*\*ADM-0[1-9]\*\*:" .planning/REQUIREMENTS.md | grep -q "^9$"
      grep -cE "^\| ADM-0[1-9] \| Phase 10 \|" .planning/REQUIREMENTS.md | grep -q "^9$"
      grep -q "v2.0 requirements: 36 total" .planning/REQUIREMENTS.md
      grep -q "### Ad Monetisation" .planning/REQUIREMENTS.md
      grep -cE "^- \[[ x]\] \*\*ADS-0[1-4]\*\*:" .planning/REQUIREMENTS.md | grep -q "^4$"
      grep -cE "^- \[[ x]\] \*\*DON-0[1-6]\*\*:" .planning/REQUIREMENTS.md | grep -q "^6$"
    </automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md has new `### Ad Monetisation` section with 9 ADM-* tickbox entries
    - REQUIREMENTS.md Traceability table contains 9 rows mapping ADM-01..09 to Phase 10
    - Coverage block updated: total = 36, mapped = 36, last updated = 2026-04-27
    - All pre-existing entries (THME-01..03, CARD-01..02, TYPO-01..02, ART-01..03, INTR-01..04, PLSH-01..03, ADS-01..04, DON-01..06) are preserved verbatim
    - File is valid markdown — no broken table rows or unclosed sections
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md has all 9 ADM-* entries with tickboxes + traceability rows. Coverage incremented from 27 to 36.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks:
- `grep -c "DIRECT" public/ads.txt` returns 4 (or 2 if Sovrn deferred)
- `grep -c "<url>" public/sitemap.xml` returns 2
- `grep -c "ADM-0" .planning/REQUIREMENTS.md` returns at least 18 (9 in section + 9 in table)
- `find . -name ads.txt -not -path "*/node_modules/*" -not -path "*/.git/*"` returns exactly `./public/ads.txt`
- No file outside `public/` and `.planning/` is modified
</verification>

<success_criteria>
1. ads.txt has all required publisher records, single source of truth confirmed
2. sitemap.xml lists /privacy
3. robots.txt unchanged (audit pass)
4. REQUIREMENTS.md backfilled with ADM-01..09 + traceability + updated coverage counts
5. No `client/public/ads.txt` duplicate created
</success_criteria>

<output>
After completion, create `.planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-02-SUMMARY.md` documenting:
- Final ads.txt line count (4 or 2 depending on Sovrn deferral status)
- Sitemap URL count (should be 2)
- REQUIREMENTS.md ADM-01..09 backfilled (yes/no)
- Confirmation: no client/public/ads.txt duplicate exists
</output>
