---
phase: 10-replace-adsense-with-adsterra-banner-and-media-net
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - client/.env
  - .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/MEDIA-NET-SUBMISSION.md
autonomous: false
requirements: [ADM-01, ADM-07]
user_setup:
  - service: adsterra
    why: "Primary display ad network — provides VITE_ADSTERRA_UNIT_KEY, VITE_ADSTERRA_PUB_ID, VITE_ADSTERRA_TAG_HASH"
    env_vars:
      - name: VITE_ADSTERRA_UNIT_KEY
        source: "Adsterra Publisher Dashboard → Websites → sunozip.com → Banner unit → Code (the alphanumeric in the invoke.js URL)"
      - name: VITE_ADSTERRA_PUB_ID
        source: "Adsterra Publisher Dashboard → Account → ads.txt (publisher ID column)"
      - name: VITE_ADSTERRA_TAG_HASH
        source: "Adsterra Publisher Dashboard → Account → ads.txt (last column of DIRECT line)"
    dashboard_config:
      - task: "Disable popunder, direct-link, social-bar, in-page-push"
        location: "Adsterra Dashboard → Websites → sunozip.com → Settings → Ad formats — confirm only Banner is checked"
  - service: sovrn-commerce
    why: "Affiliate-link rewriter (orthogonal to display ads) — provides VITE_SOVRN_SITE_ID, VITE_SOVRN_PUB_ID + ads.txt lines"
    env_vars:
      - name: VITE_SOVRN_SITE_ID
        source: "platform.sovrn.com → Site settings → Signal IID"
      - name: VITE_SOVRN_PUB_ID
        source: "platform.sovrn.com → Account (publisher ID for ads.txt lines)"
  - service: iubenda
    why: "Free-tier privacy policy generator — required by Sovrn ToS + Media.net + future AdSense reapply"
    env_vars:
      - name: IUBENDA_POLICY_ID
        source: "iubenda.com Dashboard → Privacy policy → embed code (numeric ID in iubenda.com/privacy-policy/{id} URL)"
        note: "Captured in plan 02 hardcoded reference; not VITE_-bundled because the URL is public anyway"
  - service: media-net
    why: "Secondary publisher application — non-blocking; Adsterra approval is sufficient to ship"
    dashboard_config:
      - task: "Submit publisher application via media.net web form using sunozip.com"
        location: "https://www.media.net/ — Become a Publisher form"

must_haves:
  truths:
    - "User has an approved Adsterra publisher account for sunozip.com with ONLY Banner format enabled"
    - "User has a Sovrn Commerce account producing valid ads.txt lines for lijit.com + sovrn.com"
    - "User has generated an Iubenda free-tier privacy policy and obtained the embed snippet + numeric policy ID"
    - "Media.net publisher application has been submitted with a confirmation/ticket ID logged"
    - "client/.env is populated with all five VITE_* values and is gitignored (not committed)"
  artifacts:
    - path: ".planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/MEDIA-NET-SUBMISSION.md"
      provides: "Media.net submission record (ADM-07)"
      contains: "Submitted, Ticket ID, Status fields"
    - path: "client/.env"
      provides: "Real Adsterra + Sovrn credentials for production build"
      contains: "VITE_ADSTERRA_UNIT_KEY="
  key_links:
    - from: "client/.env"
      to: "AdSlot.tsx"
      via: "import.meta.env.VITE_ADSTERRA_UNIT_KEY (consumed at App.tsx call site, plan 06)"
      pattern: "VITE_ADSTERRA_UNIT_KEY"
    - from: "Adsterra dashboard"
      to: "public/ads.txt (plan 02)"
      via: "publisher ID + tag hash copied from dashboard into ads.txt placeholders"
      pattern: "adsterra.com,"
---

<objective>
Wave 0 user-action gate. Land four account registrations and one .env population so all downstream code plans (Wave 1+) have real credentials to consume. Without this wave, AdSlot renders empty (no key), ads.txt validation fails (placeholders), and Sovrn signal fires against an invalid IID.

Purpose: ADM-01 (Adsterra banner-only configuration in dashboard) + ADM-07 (Media.net submission record) are intrinsically human-only — neither has a CLI/API the planner can automate. Iubenda + Sovrn registrations also produce values that only a human can fetch from a third-party UI. Bundle them here so all Wave 1+ tasks can assume credentials exist.

Output:
- One submission-log artifact (`MEDIA-NET-SUBMISSION.md`) committed to the repo.
- One populated `client/.env` (gitignored — NOT committed) with five VITE_* keys + IUBENDA_POLICY_ID note.
- Four Adsterra dashboard ad formats disabled (Banner only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-CONTEXT.md
@.planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-RESEARCH.md
@.planning/research/ad-networks-vs-adsense.md

<interfaces>
<!-- Env-var keys and their consumers (downstream Wave 1/2 plans) -->
client/.env keys (gitignored):
  VITE_ADSTERRA_UNIT_KEY  → consumed by App.tsx via import.meta.env.VITE_ADSTERRA_UNIT_KEY (passed to AdSlot.adKey prop)
  VITE_ADSTERRA_PUB_ID    → manually copied into public/ads.txt placeholder YOUR_ADSTERRA_PUB_ID
  VITE_ADSTERRA_TAG_HASH  → manually copied into public/ads.txt placeholder YOUR_ADSTERRA_TAG_HASH
  VITE_SOVRN_SITE_ID      → manually copied into client/index.html + public/index.html script-src URL placeholder YOUR_SOVRN_SITE_ID
  VITE_SOVRN_PUB_ID       → manually copied into public/ads.txt placeholders YOUR_SOVRN_PUB_ID (both lijit.com + sovrn.com lines)

IUBENDA_POLICY_ID (note in .env, not bundled): used as the numeric ID in client/src/pages/Privacy.tsx anchor href https://www.iubenda.com/privacy-policy/{id}
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User registers Adsterra publisher account and disables non-banner formats</name>
  <read_first>
    - .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-CONTEXT.md (Display network selection — LOCKED + Pitfall 7 in research §3.1)
    - .planning/research/ad-networks-vs-adsense.md §3.1 (Adsterra deep dive) and §8 Pitfall 7 (popunder enabled by default)
    - .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-RESEARCH.md "User Constraints" → "Display network selection"
  </read_first>
  <what-built>
    Nothing built yet — this is a human-only gate. Adsterra has no API for account creation or format toggling.
  </what-built>
  <how-to-verify>
    1. Visit https://adsterra.com/publishers and create a publisher account using sunozip.com.
    2. Wait 5-10 min for site approval (typical Adsterra SLA).
    3. After approval, log in to the Adsterra dashboard → Websites → sunozip.com → Settings → Ad formats.
    4. CRITICAL: ensure ONLY `Banner` is checked. Uncheck `Popunder`, `Direct Link`, `Social Bar`, `In-Page Push` if they are enabled (Adsterra enables all by default — Pitfall 7).
    5. Create a Banner ad unit at 728x90 leaderboard format. Note the unit's invoke.js URL.
    6. Take a screenshot of the Ad formats settings page showing only Banner enabled. Save it as `.planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/adsterra-formats-screenshot.png` (or note the dashboard state in the resume signal — screenshot strongly preferred for ADM-01 audit).
    7. Capture three values from dashboard:
       - Adsterra unit key (from invoke.js URL: `//www.highperformanceformat.com/{KEY}/invoke.js`)
       - Adsterra publisher ID (from Account → ads.txt page)
       - Adsterra tag hash (from Account → ads.txt page, last column of DIRECT line)
  </how-to-verify>
  <acceptance_criteria>
    - Adsterra dashboard shows sunozip.com status = "Approved" or "Active"
    - Adsterra dashboard Ad formats: only `Banner` checkbox is checked; `Popunder`, `Direct Link`, `Social Bar`, `In-Page Push` are unchecked
    - User has captured (and is ready to paste into client/.env in Task 4): Adsterra unit key, publisher ID, tag hash
  </acceptance_criteria>
  <resume-signal>
    Type "approved" along with the three Adsterra values pasted in the format:
    `VITE_ADSTERRA_UNIT_KEY=...`
    `VITE_ADSTERRA_PUB_ID=...`
    `VITE_ADSTERRA_TAG_HASH=...`
    OR describe blockers (rejected, dashboard issues, etc.)
  </resume-signal>

  <threat_model>
    **T-10-formats** (STRIDE: Tampering — advertiser-controlled formats degrade UX):
    - Disposition: mitigate
    - Mitigation: Banner-only enforced manually in Adsterra dashboard. Any popunder/social-bar firing in production is detected by Wave 3 incognito UAT (plan 07 task) — 60-second observation window.
    - Severity: MEDIUM (does not block planning per security_enforcement gate "high" threshold; documented for execution-time enforcement)
  </threat_model>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: User registers Sovrn Commerce account and captures site ID + ads.txt lines</name>
  <read_first>
    - .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-CONTEXT.md (Sovrn Commerce wiring — LOCKED)
    - .planning/research/ad-networks-vs-adsense.md §3.5 (Sovrn Commerce integration) + §9.1 (ads.txt lines)
    - .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-RESEARCH.md Findings §4 (Sovrn ads.txt format verification)
  </read_first>
  <what-built>
    Nothing built — human-only registration. Sovrn provides values; values consumed in Wave 1 plans 02 (ads.txt) and 04 (head script).
  </what-built>
  <how-to-verify>
    1. Visit https://www.sovrn.com/commerce/ and submit a publisher application using sunozip.com.
    2. After approval (typically days, not minutes — Sovrn is more selective than Adsterra):
       - Sovrn dashboard → Site settings → copy the Signal IID (this is `VITE_SOVRN_SITE_ID`).
       - Sovrn dashboard → Advertising → Ads.txt → copy the publisher ID (this is `VITE_SOVRN_PUB_ID`).
       - Confirm Sovrn provides BOTH a `lijit.com,...` and a `sovrn.com,...` line. The hash `fafdf38b16bf6b2b` should be the cert authority hash for both lines (verified per RESEARCH §9.1 + Findings §4).
    3. Privacy policy URL required at activation: provide `https://sunozip.com/privacy` (will be live after Wave 2).
  </how-to-verify>
  <acceptance_criteria>
    - Sovrn dashboard shows sunozip.com approved
    - User has captured `VITE_SOVRN_SITE_ID` (the Signal IID — pasted into client/index.html script-src URL in Wave 1 plan 04)
    - User has captured `VITE_SOVRN_PUB_ID` (used in BOTH lijit.com and sovrn.com lines of public/ads.txt in Wave 1 plan 02)
    - Sovrn ads.txt panel confirms both lijit.com + sovrn.com lines required for this account
  </acceptance_criteria>
  <resume-signal>
    Type "approved" with values pasted as:
    `VITE_SOVRN_SITE_ID=...`
    `VITE_SOVRN_PUB_ID=...`
    OR "deferred" if Sovrn approval pending (plan 04 head-script task can proceed with placeholder; plan 02 ads.txt should hold off Sovrn lines until approved — see plan 02 task 1 acceptance criteria).
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: User generates Iubenda free-tier privacy policy and captures embed code + numeric ID</name>
  <read_first>
    - .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-CONTEXT.md (Privacy policy page — LOCKED)
    - .planning/research/ad-networks-vs-adsense.md §10 (AdSense reapply checklist — Iubenda standard embedding required)
    - .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-RESEARCH.md Pattern 4 (Iubenda standard embedding code example)
  </read_first>
  <what-built>
    Nothing built — human-only registration. Iubenda provides numeric policy ID; consumed in Wave 1 plan 03 (Privacy.tsx) anchor href.
  </what-built>
  <how-to-verify>
    1. Visit https://www.iubenda.com/ → sign up for free tier (≤1K pageviews/month).
    2. Create a new Privacy Policy for sunozip.com:
       - Site URL: https://sunozip.com
       - Hosting country: (user's choice; suggest US)
       - Add data-collection clauses for: Adsterra (cookies/IP for ad serving), Sovrn (outbound link tracking), AdSense (kept inert but disclose for future), Express session (downloader settings persistence).
    3. After generation, capture the policy URL: `https://www.iubenda.com/privacy-policy/{NUMERIC_ID}` — record the numeric ID.
    4. Capture the embed snippet provided by Iubenda (anchor + script tag pair). Save the snippet for Wave 1 plan 03 reference.
  </how-to-verify>
  <acceptance_criteria>
    - User has the numeric Iubenda policy ID (e.g., 12345678) — used in `client/src/pages/Privacy.tsx` href in Wave 1 plan 03
    - User has copied the Iubenda standard embedding snippet (`<a class="iubenda-white iubenda-noiframe iubenda-embed iubenda-noiframe" href="https://www.iubenda.com/privacy-policy/{id}">Privacy Policy</a>`) for reference
    - Embed style class confirmed as `iubenda-white iubenda-noiframe iubenda-embed iubenda-noiframe` (note: research §6 + UI-SPEC component contract use this exact class string)
  </acceptance_criteria>
  <resume-signal>
    Type "approved" with the numeric policy ID:
    `IUBENDA_POLICY_ID=12345678`
    OR describe blockers.
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: User submits Media.net publisher application and creates submission-log artifact</name>
  <read_first>
    - .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-CONTEXT.md (Media.net submission — LOCKED)
    - .planning/research/ad-networks-vs-adsense.md §3.2 (Media.net submission requirements)
    - .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-PATTERNS.md (MEDIA-NET-SUBMISSION.md template)
    - .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-RESEARCH.md Example 7 (template)
  </read_first>
  <what-built>
    A new markdown artifact `.planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/MEDIA-NET-SUBMISSION.md` containing the submission record. This is committed to git; ADM-07 verifies its existence.
  </what-built>
  <how-to-verify>
    1. Visit https://www.media.net/ → "Become a Publisher" form → submit application using sunozip.com:
       - Site URL: https://sunozip.com
       - Privacy policy URL: https://sunozip.com/privacy (state "live within 24h" if not yet deployed)
       - Disclose simultaneous Adsterra display (Media.net allows; verified per CONTEXT.md research §3.2)
       - Disclose AdSense currently rejected (transparent application history)
    2. Capture the confirmation/ticket ID from the response email or post-submission page.
    3. Create `.planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/MEDIA-NET-SUBMISSION.md` with EXACTLY this content (fill placeholders with real values):

    ```markdown
    # Media.net Publisher Application — sunozip.com

    **Submitted:** 2026-04-27 (or actual date)
    **Account email:** {email used for Media.net signup}
    **Ticket ID:** {confirmation number from Media.net}
    **Submission method:** https://www.media.net/ → Become a Publisher form
    **Domain submitted:** https://sunozip.com
    **Application status:** submitted

    ## Notes

    - Domain age at submission: {N days from 2026-04-14 (sunozip.com registration date)}
    - AdSense status declared: rejected, kept inert in head + ads.txt for future reapply (target 2026-Q4)
    - Adsterra simultaneous-display disclosed: yes (no exclusivity conflict per Media.net ToS — research §3.2)
    - Privacy policy URL submitted: https://sunozip.com/privacy

    ## Resolution

    [fill in when decision arrives — out-of-band, not phase-blocking]
    ```

    4. Save file. Commit will happen at end of Wave 0 (deferred — git workflow handles).
  </how-to-verify>
  <acceptance_criteria>
    - File `.planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/MEDIA-NET-SUBMISSION.md` exists
    - File contains a real "Submitted:" date (not placeholder)
    - File contains a real "Ticket ID:" value (not placeholder)
    - Verifiable: `test -f .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/MEDIA-NET-SUBMISSION.md && grep -E "^\*\*Ticket ID:\*\* [^\{]" .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/MEDIA-NET-SUBMISSION.md` exits 0
    - File contains "**Application status:** submitted" or "acknowledged"
  </acceptance_criteria>
  <resume-signal>
    Type "approved" once MEDIA-NET-SUBMISSION.md exists with real values.
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: User populates client/.env with all five VITE_* values and confirms gitignored</name>
  <read_first>
    - .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-CONTEXT.md (Environment variables — LOCKED)
    - .planning/research/ad-networks-vs-adsense.md §9.5 (env-var spec)
    - .gitignore (root) line 2 — confirms `.env` is gitignored
  </read_first>
  <what-built>
    `client/.env` populated with real credentials from tasks 1-3. File is gitignored; never committed.
  </what-built>
  <how-to-verify>
    1. Create `client/.env` (or edit if exists) with EXACTLY these five lines (replace `...` with real values from tasks 1-3):

    ```env
    VITE_ADSTERRA_UNIT_KEY=...
    VITE_ADSTERRA_PUB_ID=...
    VITE_ADSTERRA_TAG_HASH=...
    VITE_SOVRN_SITE_ID=...
    VITE_SOVRN_PUB_ID=...
    ```

    2. Optionally add (not bundled but useful):
    ```env
    # IUBENDA_POLICY_ID is hardcoded in client/src/pages/Privacy.tsx — record here for reference only
    # IUBENDA_POLICY_ID=...
    ```

    3. Verify gitignored:
       - Run `git check-ignore client/.env` — must exit 0 (file is ignored).
       - Run `git ls-files client/.env` — must return empty (file is NOT tracked).

    4. Verify Vite picks up the file — run `cd client && npm run build` and confirm build exits 0 with no errors about missing env vars (build is permissive; `import.meta.env.VITE_*` is `undefined` if missing, AdSlot's `?? ''` handles that).
  </how-to-verify>
  <acceptance_criteria>
    - File `client/.env` exists at repo `client/.env`
    - `cat client/.env | grep -cE "^VITE_(ADSTERRA_UNIT_KEY|ADSTERRA_PUB_ID|ADSTERRA_TAG_HASH|SOVRN_SITE_ID|SOVRN_PUB_ID)=" ` returns exactly 5
    - `git ls-files client/.env` returns empty (file not tracked)
    - `git check-ignore client/.env` exits 0 (file gitignored)
    - No VITE_* value contains placeholder text — verifiable: `! grep -E '^VITE_.*=(replace-with-|your-|YOUR_|placeholder)' client/.env` exits 0 (i.e., grep finds zero matches; real values from tasks 1-3 substituted)
    - `cd client && npm run build` exits 0
  </acceptance_criteria>
  <resume-signal>
    Type "approved" once `client/.env` has real values and gitignore is verified.
  </resume-signal>

  <threat_model>
    **T-10-envleak** (STRIDE: Information Disclosure — env vars accidentally committed):
    - Disposition: mitigate
    - Mitigation: `.gitignore` line 2 already excludes `.env`; verification step `git ls-files client/.env` (must return empty) catches accidental staging. Note: Adsterra/Sovrn keys are PUBLIC (visible in network tab on production) — this is tidiness, not security per CONTEXT.md.
    - Severity: LOW-MEDIUM (does not block; documented for execution-time enforcement)
  </threat_model>
</task>

</tasks>

<verification>
After all 5 tasks complete:
- `cat .planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/MEDIA-NET-SUBMISSION.md` shows real ticket ID + date
- `git ls-files client/.env` returns empty (not tracked)
- `cat client/.env | grep -c "^VITE_"` returns 5
- Adsterra dashboard screenshot or user confirmation that only Banner format is enabled
- All four downstream Wave 1 plans (02-05) can proceed because their env-var placeholders have real values to substitute
</verification>

<success_criteria>
1. Five user-action checkpoints reach "approved" resume signal
2. Two repo artifacts created: `client/.env` (gitignored) + `MEDIA-NET-SUBMISSION.md` (committed)
3. Adsterra dashboard config locked to banner-only
4. Wave 1 plans (02-05) unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/10-replace-adsense-with-adsterra-banner-and-media-net/10-01-SUMMARY.md` documenting:
- Adsterra approval date + screenshot path (if captured)
- Sovrn approval date (or "deferred — Adsterra-only" if user opted not to apply)
- Iubenda policy ID
- Media.net ticket ID
- Confirmation that client/.env is populated and gitignored
</output>
