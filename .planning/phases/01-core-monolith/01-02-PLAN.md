---
phase: 01-core-monolith
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - client/src/App.css
  - client/src/App.tsx
  - client/src/components/ThemeToggle.tsx
  - client/public/assets/logo.png
autonomous: true
requirements:
  - CARD-01
  - CARD-02
  - TYPO-01
  - TYPO-02

must_haves:
  truths:
    - "All content sections sit inside 24px radius cards that visually lift from the page"
    - "Dark mode cards show inner glow + soft shadow; light mode cards show clean drop shadow"
    - "App title renders at 24px semi-bold, section headings at 20px semi-bold, body at 14px regular"
    - "Inter font is active with correct letter-spacing at each hierarchy level"
    - "Accent color #3B4A6B applied consistently to buttons, focus rings, badges, progress bar, and logo"
    - "Mantine components (Button, Progress, Badge) respect the accent color via CSS overrides"
  artifacts:
    - path: "client/src/App.css"
      provides: "Monolith card class, typography hierarchy, Mantine overrides, accent button states"
      contains: ".monolith-card"
    - path: "client/src/App.tsx"
      provides: "Updated className references from glass-card to monolith-card"
      contains: "monolith-card"
    - path: "client/public/assets/logo.png"
      provides: "Generated app logo with transparent background"
  key_links:
    - from: "client/src/App.css"
      to: "client/src/index.css"
      via: "var() references to CSS custom properties"
      pattern: "var\\(--bg-card\\)"
    - from: "client/src/App.tsx"
      to: "client/src/App.css"
      via: "className='monolith-card'"
      pattern: "monolith-card"
---

<objective>
Apply the Monolith card system (24px radius, mode-aware depth), typography hierarchy (Inter at 4 sizes), accent color consistency, and logo to all UI components. This transforms the visual identity from glassmorphism to the Monolith solid-surface aesthetic.

Purpose: With the palette foundation in place from Plan 01, this plan applies it to every visible element -- cards, headings, buttons, badges, and the logo.
Output: Complete visual transformation matching the Monolith design system.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-monolith/01-CONTEXT.md
@.planning/phases/01-core-monolith/01-RESEARCH.md
@.planning/phases/01-core-monolith/01-UI-SPEC.md
@.planning/phases/01-core-monolith/01-01-SUMMARY.md

<interfaces>
<!-- CSS variables from Plan 01 (now set in index.css) -->
<!-- These are the vars that App.css classes consume: -->
<!--
  --bg-card: #1A1A1A (dark) / #E8E4DB (light)
  --bg-card-hover: #222222 (dark) / #DDD9D0 (light)
  --border-color: rgba(255,255,255,0.08) (dark) / rgba(0,0,0,0.08) (light)
  --shadow-card: inset glow+shadow (dark) / drop shadow (light)
  --accent: #3B4A6B
  --accent-hover: #4E5F80
  --accent-active: #2F3D58
  --accent-glow: rgba(59,74,107,0.25) (dark) / rgba(59,74,107,0.15) (light)
  --shadow-button: 0 2px 8px rgba(59,74,107,0.3) (dark) / rgba(59,74,107,0.2) (light)
  --text-primary, --text-secondary, --text-muted (mode-aware)
-->

From client/src/App.tsx (line 269 -- the glass-card usage):
```tsx
<div className="glass-card" style={{
    marginBottom: "24px",
    padding: "4px",
    maxHeight: "320px",
    overflowY: "auto"
}}>
```

From client/src/App.tsx (line 234 -- logo area):
```tsx
<div className="app-logo">
    <IconVinyl size={22} color="#ffffff" stroke={1.5} />
</div>
```

From client/src/App.css (current .glass-card):
```css
.glass-card {
  background: var(--bg-card);
  backdrop-filter: var(--glass-blur);
  -webkit-backdrop-filter: var(--glass-blur);
  border: 1px solid var(--glass-border);
  border-radius: 14px;
  box-shadow: var(--shadow-card);
  transition: background 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Monolith card class, typography hierarchy, and accent consistency in App.css</name>
  <files>client/src/App.css</files>
  <read_first>
    - client/src/App.css (full current state -- all classes to be modified)
    - client/src/index.css (CSS variables available after Plan 01)
    - .planning/phases/01-core-monolith/01-UI-SPEC.md (card contract, typography, interaction states)
    - .planning/phases/01-core-monolith/01-CONTEXT.md (decisions D-01 through D-08)
  </read_first>
  <action>
**Card rename and update** -- Replace `.glass-card` with `.monolith-card` per D-03:

```css
.monolith-card {
  background: var(--bg-card);
  border: 1px solid var(--border-color);
  border-radius: 24px;
  box-shadow: var(--shadow-card);
  transition: background-color 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease;
}
```

Remove `.glass-card:hover` block entirely. Do NOT add a `.monolith-card:hover` block (card hover is Phase 3 scope per D-08).

Remove `backdrop-filter: var(--glass-blur)` and `-webkit-backdrop-filter: var(--glass-blur)` -- Monolith uses solid surfaces per D-01/D-02.

Also update `.info-banner` to remove its `backdrop-filter` lines (268-269 in current App.css) and update its border-radius to 24px:

```css
.info-banner {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 14px 18px;
  margin-bottom: 28px;
  border-radius: 24px;
  background: var(--bg-card);
  border: 1px solid var(--border-color);
}
```

Update `.support-banner` border-radius to 24px (was 10px):

```css
.support-banner {
  /* keep existing flex/align/gap/padding/margin */
  border-radius: 24px;
  /* keep existing background, border, transition */
}
```

**Typography hierarchy** -- Per D-04 and D-06:

Update `.app-title`:
```css
.app-title {
  margin: 0;
  font-size: 24px;
  font-weight: 600;
  letter-spacing: -0.03em;
  color: var(--text-primary);
}
```
Changed from: 18px -> 24px, 700 -> 600, -0.02em -> -0.03em (per UI-SPEC revision: 600 weight at 24px is visually prominent without needing 800).

Update `.section-heading`:
```css
.section-heading {
  color: var(--text-primary);
  font-size: 20px;
  font-weight: 600;
  margin: 24px 0 12px;
  letter-spacing: -0.02em;
}
```
Changed from: 16px -> 20px, letter-spacing -0.01em -> -0.02em.

Song table body text is already 14px regular (correct per D-04). Table headers already have `letter-spacing: 0.06em` and `text-transform: uppercase` (correct per D-06 caption treatment).

**Button accent states** -- Per D-08:

Update `.btn-accent`:
```css
.btn-accent {
  background: var(--accent);
  color: #ffffff;
  border: none;
  border-radius: 10px;
  padding: 11px 22px;
  font-weight: 600;
  font-size: 14px;
  cursor: pointer;
  box-shadow: var(--shadow-button);
  transition: background-color 0.2s ease, box-shadow 0.2s ease, transform 0.15s ease;
}
```

Update `.btn-accent:hover:not(:disabled)`:
```css
.btn-accent:hover:not(:disabled) {
  background: var(--accent-hover);
  box-shadow: var(--shadow-button);
  transform: translateY(-1px);
}
```
Changed: removed `--shadow-button-hover` reference (variable no longer exists), use same `--shadow-button`. No glow on hover per D-08.

Add `.btn-accent:active:not(:disabled)` (new per D-08):
```css
.btn-accent:active:not(:disabled) {
  background: var(--accent-active);
  transform: translateY(0);
}
```

**Logo styling** -- Update `.app-logo` for the generated PNG logo (replacing gradient background + icon):
```css
.app-logo {
  width: 38px;
  height: 38px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.app-logo img {
  width: 38px;
  height: 38px;
  border-radius: 11px;
}
```
Remove the old gradient background: `background: linear-gradient(135deg, #4a90ff, #7c5cff)` and old `box-shadow: 0 2px 12px rgba(74, 144, 255, 0.3)`.

If logo generation fails (Task 3 fallback), the `.app-logo` will contain an IconVinyl wrapped in a div with accent-colored background:
```css
.app-logo-fallback {
  background: var(--accent);
  border-radius: 11px;
  width: 38px;
  height: 38px;
  display: flex;
  align-items: center;
  justify-content: center;
}
```

**Mantine component overrides** -- Per RESEARCH.md Pitfall 4 and UI-SPEC:

Add these CSS rules at the end of App.css (after the animations section):

```css
/* ─── Mantine v6 accent color overrides ─── */
.mantine-Button-root[data-variant="filled"] {
  background-color: var(--accent) !important;
}
.mantine-Button-root[data-variant="filled"]:hover {
  background-color: var(--accent-hover) !important;
}
.mantine-Button-root[data-variant="filled"]:active {
  background-color: var(--accent-active) !important;
}

.mantine-Progress-bar {
  background-color: var(--accent) !important;
}

.mantine-Badge-root[data-variant="filled"] {
  background-color: var(--accent) !important;
}

.mantine-ActionIcon-root {
  color: var(--text-secondary) !important;
  transition: color 0.2s ease, background-color 0.2s ease !important;
}
```

**Transition declarations** -- Per D-10, add transition to `.app-wrapper`:

The `.app-wrapper` already has `transition: background-color 0.4s ease, color 0.3s ease;`. This is correct. Keep it.

Add transition to `.support-banner`:
Already has `transition: background 0.3s ease, border-color 0.3s ease;`. Keep it.
  </action>
  <verify>
    <automated>cd /Users/ashley.rhodes/proj/scripts/learning-projects/suno-playlist-downloader && grep -c "monolith-card" client/src/App.css && ! grep -q "glass-card" client/src/App.css && ! grep -q "backdrop-filter" client/src/App.css && ! grep -q "glass-blur" client/src/App.css && grep -q "font-size: 24px" client/src/App.css && grep -q "font-size: 20px" client/src/App.css && grep -q "accent-active" client/src/App.css && grep -q "mantine-Button-root" client/src/App.css && yarn build</automated>
  </verify>
  <acceptance_criteria>
    - client/src/App.css contains `.monolith-card` with `border-radius: 24px`
    - client/src/App.css does NOT contain `.glass-card`
    - client/src/App.css does NOT contain `backdrop-filter`
    - client/src/App.css does NOT contain `glass-blur`
    - client/src/App.css `.app-title` has `font-size: 24px` and `font-weight: 600` and `letter-spacing: -0.03em`
    - client/src/App.css `.section-heading` has `font-size: 20px` and `font-weight: 600` and `letter-spacing: -0.02em`
    - client/src/App.css `.btn-accent:active` references `var(--accent-active)`
    - client/src/App.css contains `.mantine-Button-root[data-variant="filled"]` override
    - client/src/App.css contains `.mantine-Progress-bar` override
    - client/src/App.css contains `.mantine-Badge-root[data-variant="filled"]` override
    - client/src/App.css `.info-banner` has `border-radius: 24px` and no `backdrop-filter`
    - client/src/App.css `.support-banner` has `border-radius: 24px`
    - client/src/App.css `.app-logo` does NOT contain `linear-gradient`
    - `yarn build` exits 0
  </acceptance_criteria>
  <done>All cards use 24px radius with mode-aware depth shadows. Typography hierarchy matches D-04 spec. Accent color applied consistently. Mantine component overrides in place. No glass-morphism artifacts remain. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Update App.tsx class references and ThemeToggle accent</name>
  <files>client/src/App.tsx, client/src/components/ThemeToggle.tsx</files>
  <read_first>
    - client/src/App.tsx (line 269 -- glass-card className, line 234 -- logo area)
    - client/src/components/ThemeToggle.tsx (hardcoded color values)
    - .planning/phases/01-core-monolith/01-CONTEXT.md (D-07 accent consistency, D-09 logo)
  </read_first>
  <action>
**client/src/App.tsx** -- Three changes:

1. Rename `className="glass-card"` to `className="monolith-card"` on line 269:
```tsx
<div className="monolith-card" style={{
    marginBottom: "24px",
    padding: "4px",
    maxHeight: "320px",
    overflowY: "auto"
}}>
```

2. Update the logo area (lines 233-236). Replace the IconVinyl icon with the generated logo image. If logo.png exists in `client/public/assets/`:
```tsx
<div className="app-logo">
    <img src="/assets/logo.png" width={38} height={38} alt="Suno Downloader" />
</div>
```

If logo generation failed (Task 3), use the fallback:
```tsx
<div className="app-logo-fallback">
    <IconVinyl size={22} color="#ffffff" stroke={1.5} />
</div>
```

3. Remove the hardcoded gradient color reference `#ffffff` from the icon -- it was tied to the old blue gradient logo background. The new logo image handles its own colors.

**client/src/components/ThemeToggle.tsx** -- Per D-07, update the hardcoded color values to use the accent color:

Replace the current inline `style` object (line 23):
```typescript
style={{
  backgroundColor: theme === 'light' ? 'rgba(0, 113, 227, 0.1)' : 'rgba(255, 213, 0, 0.1)',
  transition: 'all 0.2s ease',
}}
```

With accent-based styling:
```typescript
style={{
  backgroundColor: 'var(--bg-card)',
  border: '1px solid var(--border-color)',
  transition: 'all 0.2s ease',
}}
```

Update the `color` prop on ActionIcon from `{theme === 'light' ? 'blue' : 'yellow'}` to remove it entirely (let CSS handle via `.mantine-ActionIcon-root` override from Task 1). If Mantine v6 requires the color prop, set it to `"gray"` as a neutral base that the CSS override will supersede:
```typescript
<ActionIcon
  variant="subtle"
  color="gray"
  onClick={toggleTheme}
  size="lg"
  radius="md"
  aria-label="Toggle color scheme"
  style={{
    backgroundColor: 'var(--bg-card)',
    border: '1px solid var(--border-color)',
    transition: 'all 0.2s ease',
  }}
>
```

Change `variant` from `"light"` to `"subtle"` to reduce Mantine's own background injection.
  </action>
  <verify>
    <automated>cd /Users/ashley.rhodes/proj/scripts/learning-projects/suno-playlist-downloader && grep -q "monolith-card" client/src/App.tsx && ! grep -q "glass-card" client/src/App.tsx && ! grep -q "rgba(0, 113, 227" client/src/components/ThemeToggle.tsx && yarn build</automated>
  </verify>
  <acceptance_criteria>
    - client/src/App.tsx contains `className="monolith-card"` (not `glass-card`)
    - client/src/App.tsx does NOT contain `className="glass-card"`
    - client/src/App.tsx contains `logo.png` or `app-logo-fallback` (one or the other depending on logo gen)
    - client/src/components/ThemeToggle.tsx does NOT contain `rgba(0, 113, 227`
    - client/src/components/ThemeToggle.tsx does NOT contain `rgba(255, 213, 0`
    - client/src/components/ThemeToggle.tsx contains `var(--bg-card)` or `var(--border-color)`
    - `yarn build` exits 0
  </acceptance_criteria>
  <done>All className references updated from glass-card to monolith-card. ThemeToggle uses CSS variable-based styling instead of hardcoded colors. Logo either uses generated PNG or accent-colored fallback. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Generate app logo via nanobanana MCP</name>
  <files>client/public/assets/logo.png</files>
  <read_first>
    - .planning/phases/01-core-monolith/01-CONTEXT.md (D-09 logo specification)
    - CLAUDE.md (ImageMagick chromakey workflow)
  </read_first>
  <action>
Per D-09, generate an app logo using the nanobanana MCP tool with chromakey green background, then remove the background with ImageMagick.

**Step 1: Generate logo**
Call nanobanana MCP `generate_image` with a prompt along these lines (Claude has design discretion per D-09):
"A minimalist vinyl record icon, flat design, white and light gray colors, on a solid chromakey green (#00FF00) background, clean lines, suitable as a 38x38 app icon, 256x256 resolution"

Save the output to `/tmp/logo-raw.png`.

**Step 2: Remove chromakey background**
Run ImageMagick to remove the green background per CLAUDE.md:
```bash
magick /tmp/logo-raw.png -fuzz 15% -transparent "#00FF00" client/public/assets/logo.png
```

If green fringe remains, use the aggressive cleanup:
```bash
magick /tmp/logo-raw.png -fuzz 20% -transparent "#00FF00" -channel alpha -morphology EdgeIn Diamond:1 +channel client/public/assets/logo.png
```

**Step 3: Verify**
Check that the file exists and has reasonable size (> 1KB, < 500KB).

**Fallback:** If nanobanana MCP is unavailable OR ImageMagick (`magick`) is not installed, skip logo generation. Task 2 already includes the `.app-logo-fallback` class with accent-colored background + IconVinyl icon. The app will work correctly with either approach.

Do NOT block the plan on logo generation. If it fails, log the reason and ensure Task 2 uses the fallback path.
  </action>
  <verify>
    <automated>cd /Users/ashley.rhodes/proj/scripts/learning-projects/suno-playlist-downloader && (test -f client/public/assets/logo.png && echo "Logo generated" || echo "Logo not generated - using fallback")</automated>
  </verify>
  <acceptance_criteria>
    - EITHER client/public/assets/logo.png exists with file size > 1KB
    - OR client/src/App.tsx contains `app-logo-fallback` className (fallback path)
    - `yarn build` exits 0 regardless of logo generation outcome
  </acceptance_criteria>
  <done>Logo generated with transparent background and placed in client/public/assets/, OR fallback icon path confirmed working. Build succeeds either way.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS class names | Class names referenced in JSX -- rename must be consistent |
| Mantine internal classes | Overriding framework classes via CSS selectors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | Mantine CSS overrides via !important | accept | Overrides target specific data-variant selectors, not wildcard. Pattern already established in codebase (body bg override). Risk is CSS specificity conflicts, not security. |
| T-01-04 | Information Disclosure | Logo image in public/assets/ | accept | Logo is intentionally public. No sensitive content. Static asset served as-is. |
</threat_model>

<verification>
1. `yarn build` succeeds with zero errors
2. `grep "monolith-card" client/src/App.tsx client/src/App.css` -- present in both files
3. `grep "glass-card" client/src/App.tsx client/src/App.css` -- present in NEITHER file
4. `grep "backdrop-filter" client/src/App.css` -- NOT present
5. `grep "font-size: 24px" client/src/App.css` -- present in .app-title
6. `grep "font-size: 20px" client/src/App.css` -- present in .section-heading
7. `grep "mantine-Button-root" client/src/App.css` -- override present
8. Logo file exists at client/public/assets/logo.png OR fallback class in App.tsx
</verification>

<success_criteria>
- All content sections use 24px radius cards with solid backgrounds (no glass blur)
- Dark mode cards show inner glow + soft shadow; light mode shows clean drop shadow
- App title renders at 24px/600 weight, section headings at 20px/600 weight, body at 14px/400 weight
- Letter spacing matches D-06: hero -0.03em, headers -0.02em, body default, captions 0.06em
- Accent #3B4A6B applied to all interactive elements including Mantine overrides
- Button hover changes to #4E5F80 (no glow), active to #2F3D58, disabled at 45% opacity
- Logo either shows generated PNG or accent-colored fallback icon
- Build compiles without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-monolith/01-02-SUMMARY.md`
</output>
